# Integrations result completeness — bounded delivery

The exact integrations assignment reconciles **10 DR groups and 264 unique methods**, without duplicates, omissions or extras. Immediate owning result contracts and the necessary local effect/error branches are recorded in [contracts.json](contracts.json). This proposes completion of this lead's bounded source work, **not whole E3 acceptance**: the final account-preparation join for two generation methods is assigned to the account auditor below. Root audit remains **83.3% (10/12), medium-low confidence, delta 0**; test migration and product fidelity remain separate, unrun gates. The next milestone is root review of this delivery and the account join; 24-hour completion risk remains elevated.

The original source is the authorized read-only reference at root-supplied revision `c97906287bb7a390b25e2025b600d9fb3c25d9c3`. No Git was used to authenticate that pin. Evidence contains **1,253 inclusive range SHA256 records across 781 distinct source files**, including retained definition/handler/assertion evidence and newly read implementation bodies. Full-file fingerprints establish identity; neither hashes nor file counts establish behavior. No source modules, tests, inference, installed-app effects, services, installs, credentials or external operations were executed.

| Exact group | Methods | Final cross-owner source dependency |
|---|---:|---|
| DR-REPO_METHODS | 42 | None newly retained |
| DR-WORKTREE_METHODS | 17 | None newly retained |
| DR-FILE_METHODS | 30 | None newly retained |
| DR-GIT_METHODS | 35 | Two account-preparation joins |
| DR-GITHUB_METHODS | 50 | None newly retained |
| DR-GITLAB_METHODS | 21 | None newly retained |
| DR-HOSTED_REVIEW_METHODS | 4 | None newly retained |
| DR-LINEAR_METHODS | 25 | None newly retained |
| DR-LINEAR_AGENT_ACCESS_METHODS | 16 | None newly retained |
| DR-JIRA_METHODS | 24 | None newly retained |

Each method retains its exact `DR-ID::method`, original source-test allocation, RPC definition/handler ranges, receiver fixture and inherited F gates. Result contracts distinguish null/undefined/absence, coercion, state writes, partial success, failure and local/SSH/WSL/version selection. Existing owner contracts are reused only for their recorded bodies and scope. UI presentation, accepted admission and callback registration are not substituted for service outcomes.

**Cross-owner join I-XOWNER-ACCOUNT-PREPARATION:** `git.generateCommitMessage` and `git.discoverCommitMessageModels` reach local account launch preparation before inference or model probing. The coordinator assigned the final selected account preparation join to **task_823552276b48 / ctx_dee67d480e3d**, terminal `term_c317aa5c-b92d-4338-a888-e88676e58fde`, with exclusive outputs [account-launch-final.md](../result-platform/account-launch-final.md) and [account-launch-final.json](../result-platform/account-launch-final.json). Those outputs are pending root review and were not read for this join. This report retains the already reviewed generation/discovery/startup/routing and account-storage evidence; the final nested account result is **source debt pending that owner**, not a genuine external boundary or merely an unrun test. Root must join the owner's accepted evidence before closing the affected source gate. Per direct guidance, this lead neither waits for nor duplicates that audit.

Material result distinctions:

- Worktree creation can retain Git state, metadata, retired names and lineage after later copy, setup or terminal failure. Setup `running` is not a passed hook. Prepared-worktree claiming, sparse rollback, copy budgets, remote-root registration, PR/MR base differences and lineage identity guards are characterized. RPC deduplication uses raw repository selector plus mutation ID, retains successful results for 60 seconds, drops rejection immediately and does not include host/client/changed parameters in its key.
- Removal distinguishes clean preflight refusal, watcher drain, quarantine/trash, failed Git deregistration, branch preservation and background deletion. A resolved sweep or removed metadata is not proof of process exit. History removal uses identity-derived keys and tombstone rename before delayed deletion; ambiguous host ownership preserves shared history/browser state.
- File writes and upload chunks can leave partial destination changes. SFTP, SSH and native routing have distinct failure shapes. Recursive watcher disposal can acknowledge unsubscription before physical child exit. Terminal-path provenance and native/WSL transcript reuse retain exact concurrent RRT references and evidence snapshots; those references remain pending root acceptance.
- GitHub, GitLab, Jira and Linear preserve their own provider response transformations, caches, pagination and partial-failure policies. Jira multi-site reads can retain healthy sites; sequential updates may modify earlier fields before a later failure. GitLab and GitHub base-resolution outputs differ. Hosted creation timeouts can leave remote completion unknown.
- GH/glab process success requires a successful, nontruncated result. GH only retries recognized idempotent reads; mutations are not retried under that policy. WSL login output fencing and guest process-group termination are traced through their actual adapters. Missing group identity, failed verification or lost SSH contact remains **unverifiable**, never `exited`.
- Shipped cautions remain separate from intended corrections: Linear `listTeams` does not await its permit; watcher ACK is not exit proof; the raw worktree mutation key can reuse a prior differently parameterized request; branch cleanup can preserve a checked-out branch without reporting preservation. Source characterization does not silently repair these behaviors.

**Assertion evidence and exact remaining execution:** eight lead assertion-body groups are associated with their affected methods in `leadAssertionBodies`, with inclusive source hashes. They cover folder deletion order and ambiguity; GitLab draft titles, auth-host parsing and trace excerpts; worktree create deduplication; preparation selection/path keys; and WSL termination identity/failure. These bodies were read, never run. Pure helper tests and mocked runtime/process callbacks are explicitly labeled; they do not prove real Git, provider, PTY, disk, WSL or remote completion. Leaf dispatcher assertions remain route evidence only. A method with no new service assertion association retains that limitation explicitly.

For **every assigned method**, remaining tests are the unchanged `originalSourceTestAllocation`, its retained `rpcBoundary.originalTests` and exact `receiverFixture`, plus `F-RPC-SCHEMA`, `F-RPC-ROUTE`, `F-RPC-HOST`, `F-RPC-RESULT` and `F-EXTERNAL` where inherited. Preserve all BM/R/F gates and complete original suites. Run the original baseline, port assertions faithfully, establish actual candidate behavioral RED before implementation, then GREEN and integrated acceptance. Compile/setup failure is not behavioral RED; skipped tests are not PASS; mocks of missing product behavior are not parity. Native/SSH/WSL, folder workspace, duplicate identity, cancellation, partial-write, unavailable-host and mixed-version fixtures remain required. Source defects and intended-correction fixtures must remain distinct.

The approved OpenCode leaf owned exactly 95 GitHub/GitLab/Jira methods. The lead read all 95 semantic rows and supplied missing local result joins. Its frozen report and JSON remain unchanged. Two beyond-EOF leaf anchors (`github.rateLimit`, `jira.listProjects`) are corrected only in lead evidence; the other 283 supplied anchors matched. The corrective dispatch exhausted its quota and was officially abandoned/released. Both child dispatches are settled, no owned child remains active, and release retained the external terminal with `identity_unproven`, no process action and no exit assertion.

Proposed disposition: accept this **bounded integrations source delivery with the explicit account-owner dependency**, subject to root completeness and independent source review. Keep whole E3 source acceptance open until root joins the pending account evidence and other assigned owners. Original baseline, faithful port, behavioral RED–GREEN, packaged/integrated verification and real product parity are not claimed by this audit.
