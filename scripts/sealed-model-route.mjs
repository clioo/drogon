// Fixture-only names: missing private config must fail, never select a real provider.
export const PI_PROVIDER = "drogon-sealed-fixture";
export const PI_MODEL_ID = "acceptance-only";
export const PI_MODEL = `${PI_PROVIDER}/${PI_MODEL_ID}`;
