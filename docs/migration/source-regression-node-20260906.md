# Original source regression — admitted Node capsules

2026-09-06: **41/41 capsules,286/286 original tests passed**, with zero failures, skipped/pending/todo cases or command timeouts. Execution used the original installed Vitest4.1.11, explicit Node24.19.0 and macOS arm64, three independent capsule workers at a time. This is a regression rerun, not286 newly covered tests or the whole9037-file suite. The newly admitted six mobile stream cases were established separately and repeated in this batch.

The explicit digest-bound plan is `tests/parity/batches/source-regression-node-20260906.json`; raw structured runner output is `source-regression-node-20260906.json`. Independent root verification reopened all41 result files and every assertion status, checked208 staged file/license hashes and139 distinct files against both current source bytes and Git pin `c97906287bb7a390b25e2025b600d9fb3c25d9c3`. Exact per-capsule result/stage fingerprints are in `source-regression-node-20260906-verification.json`. Node24 exposes JSON.rawJSON for the guarded serialization test.

The batch reuses the existing unchanged capsule/batch runners and previously recorded source/effect admission, verified again against pinned bytes; no test assertions, original mocks or module source were altered. It includes Bots ownership/reactive admission, remote liveness interpretation, browser placement, relay protocol/stream failure handling, durable local file writes, CLI pure parsers, simulated process results, usage wrapper routing and WSL fixture behavior. Real temporary-file and fixed synthetic Node-child tests remain distinguished from pure-function tests. No live application, provider, credentials, external Git hosting, native WSL/device or model inference was used by the reviewed test paths. In-process platform overrides are not native OS acceptance.

Excluded rather than silently mixed: two renderer capsules with separate React/runtime-link requirements, the CLI-arguments capsule's dedicated fixture-environment admission, and two cloud capsules pinned to Vitest4.1.9 (separate prior2/2 and5/5 results). The old orchestration-ask-timeout manifest mentions a historical5.0 runner; this batch actually ran its unchanged arithmetic assertions under4.1.11, as shown by the execution receipt. Historical evidence is not rewritten.

Minimal configuration differences and all per-capsule evidence limits remain. These fixtures are not a general security sandbox, hermetic full original workspace or candidate behavioral RED/GREEN. No feature implementation or installed preview changed. Audit remains10/12 (83.3%, medium-low confidence), delta0. Five audit assignments remain; actual Sol implementation follows complete root E3/E5 source acceptance.
