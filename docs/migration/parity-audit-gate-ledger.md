# Audit gate ledger — coordinator-reviewed checkpoint

**Audit remains open.** This v3 supersedes both worker drafts, which mixed
obsolete assignments, accepted metadata and future execution debt. This is
a current decision ledger, not a claim of a fully enumerated product.
The machine record contains exact owners, artifacts and remaining evidence.

## Accepted metadata — do not repeat completed censuses

Source/test allocation: 9,037 unique paths. Bridge: 87 domains, 947 channels,
953 reachable methods and 615 RPC names, with explicit attribution gaps.
CLI: 234 commands and shared parser rules. Settings: 214 fields, 187 builder
defaults, 88 core shortcut IDs plus 36 generated agent definitions. Harnesses:
36 IDs and specialized model-probe registries. Skills: eight guide projections,
nine native destinations, 36 community mappings, four task providers and ten
wire capability constants. Windows render test: its runner route is known.

These exact scopes are accepted; no statement above establishes complete
behavior, installation or runtime parity. In particular, 26 harnesses absent
from one specialized probe registry are not 26 missing harnesses.

## Remaining enumeration follow-ups

| ID | Required resolution | Owner |
| --- | --- | --- |
| E1 | Reconcile remaining named UI surfaces and correct unsupported behavioral claims before freezing cards. | UI package leads; coordinator acceptance |
| E2 | Trace 214 setting consumers or explicit internal classification; keep platform/dynamic shortcut boundaries explicit. | UI-SETTINGS + ENG-SHARED |
| E3 | Trace named dynamic/delegated methods, disposer and main-side channel attribution gaps. | ENG-IPC |
| E4 | Per-command handler value/default/required/output/error contracts, including passthrough dispatch. Shared parser census is already accepted. | ENG-CLI |
| E5 | Reconcile existing journey/platform/gap IDs against these artifacts; only source-backed omissions may become new owned follow-ups. | Coordinator |

These five rows group existing obligations; they are not an exact count of
missing behaviors. A speculative list of 17 stores/registries is not accepted
as 17 missing censuses. Absence of a filename does not prove missing coverage.
The JSON references the concrete source inventories and unresolved entries.

## Execution and acceptance remain separate gates

- Full original runner discovery/baselines with actual versions, effects and
  receipts; missing tools and skipped tests cannot pass. Windows execution is
  still owed despite its resolved route.
- Assertion-preserving ports for the complete source suite before the feature
  wave; behavioral RED then GREEN. Import/build failures are not behavioral RED.
- Source/candidate comparisons, both remote version skews, OS/SSH/folder
  boundaries, rendered journeys, screenshots, recovery and performance.
- After audit closure, Sol leads may plan bounded test-port leaf tasks. Each
  dispatch needs finite ownership and acceptance; requiring all leaves before
  leads exist would create a circular prerequisite.
- Final product acceptance requires the full original experience and existing
  Drogon changes, a verified installed Electron app, own CLI/core/service,
  reversible migration, and real Drogon orchestration without orca-cli underneath.

Genuinely preexisting pending feature work stays in its agreed postmigration
phase. That does not defer implemented Mentu/Bots/Meetings behavior, packaging,
brand fidelity or any other required part of the migration. No goal, scope,
runtime setting or product behavior is changed by this ledger.
