# Drogon rewrite

- Latest user policy, 2026-09-06, superseding Sol-led execution below: root Astra now plans, coordinates, integrates and independently validates directly with approved non-OpenAI workers. Do not launch new Sol leads. The three existing Sol leads must close their CURRENT assignments at a stable handoff, without new child corrections/tasks or manual implementation. Let active leaves finish their current Dispatch; preserve their files, inspectable evidence and exact identities. After child settlement, release/ack through Orca and report the handoff to root, then stop. Root assigns subsequent work directly, at depth 1, reusing an exact settled terminal only when supported and conflict-free. Never take over a still-active child or pretend its original depth-2 provenance changed. Keep at most three active non-OpenAI leaves: approved OpenCode GLM, OpenCode Kimi and Claude Sonnet lanes; no silent provider fallback, extra descendants or global permission changes. Queued Sol follow-ups require root re-scoping before dispatch, not automatic execution of their old lead instructions.
- Latest user policy, 2026-09-06: Sol engineering agents are leads, not manual implementation workers. Under root Astra direction they delegate code changes, source-closure preparation, test authoring/execution and extended debugging to approved non-OpenAI leaf providers, then perform focused review and acceptance checks. This overrides older per-Task "perform yourself/no children" directions, not file scope or depth limits. Preserve partial work, transfer explicit exclusive ownership, and never duplicate an active leaf's work. If delegation is unavailable, escalate instead of silently consuming OpenAI usage as the worker. Already completed work is reviewed, not rerun merely to attach a child.
- Authorized phase exception, 2026-09-06: Carlos explicitly approved starting Sol-led independent technical work while E5 publication/resources/services remain held. This supersedes the full-E5-before-Sol sequence below, not E5 acceptance or tests-first gates. Follow docs/migration/audit-to-sol-decision.md and sol-test-wave.md. No uncertain asset publication/replacement, service provisioning or real-data submission is authorized by this exception.
- Current provider update: Qwen token plan is exhausted; no new assignments/retries to that plan. Kimi K2.7 Code is user-approved via OpenCode `kimi-for-coding/kimi-for-coding`, with per-invocation `--auto` and a verified smoke before development. Do not silently choose another provider for the same model. This cloud-plan hold does not establish failure of the independent Pi + DGX Spark local test endpoint.
- Test-wave corrections: never run tests with the read-only reference as cwd; runners can write ignored caches. Reuse admitted receipts or stage a genuinely isolated closure through existing capsule tooling. Node24 is `/Users/carlos/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`; never recursively scan the home directory to rediscover it. A frozen copy or missing-import binding is test preparation, not a completed candidate port or behavioral RED. After child completion use `worker-release` exactly as documented; retained/external or unverifiable resources must NOT be force-closed with terminal commands. Record the retention and continue; release before delivery acknowledgment.

- Current scope is the approved Drogon rewrite. Follow the current Task's narrower phase, file ownership and acceptance criteria; implementation starts only after the coordinator accepts its prerequisite gates.
- Tests-first migration: inventory the complete source suite and preserve a source-test-to-contract map before feature implementation. Reuse or port tests without weakening assertions; verify the source baseline and candidate behavioral failures before implementing each capability. Compile/setup failures are not behavioral RED, skipped tests are not PASS, and mocks of missing product behavior cannot prove parity. Follow docs/migration/rewrite-parity-plan.md for the full gate.
- Progress reporting: include an estimated audit percentage, its denominator/confidence, change since the last update, next closure milestone and 24-hour deadline risk in meaningful updates. Keep audit closure, test migration and product fidelity separate. Use docs/migration/audit-progress.md; do not inflate progress by counting repeated reads, new documents or isolated passing cases as closed capability groups.
- Write only in this repository and your assigned paths. The previous implementation at `/Users/carlos/Documents/Drogon-mentu-session` is an explicitly authorized read-only migration reference, not an editing target. Never modify credentials, global settings or frozen challenge inputs.
- Coordinator owns shared files, root manifests/lockfiles, Git commits/pushes, repository operations and integration. Workers must not commit, push, open PRs, install dependencies or start background services unless explicitly assigned that scoped action.
- Use the live Orca Task/Dispatch instructions. Report failure honestly and stop after `worker_done`; settle and release all owned children before a lead completes.
- Latest user direction supersedes the earlier flat-audit restriction: five explicitly assigned Astra area leads may coordinate E1–E5 now, with at most one approved leaf each initially. Read docs/migration/audit-closure-coordination.md. Only designated leads may delegate, only through Orca, and only when the live Dispatch permits that depth. Leaf workers never delegate, including provider-native forks. Never change global settings or bypass a nesting denial through another identity/Run. If nesting is unavailable, preserve partial work and escalate; engineering leads must not fall back to manual implementation.
- Phase transition clarified by the user on 2026-09-06: the five Astra area leads are temporary and audit-only. After root accepts the complete audit gate, implementation uses root Astra → Sol area leads → approved disjoint leaf workers. Finish/review existing audit deliveries and settle/release their children before transfer; do not relabel live Astra sessions as Sol or start feature implementation on report completion alone. Root retains architecture, integration, independent testing and acceptance.
- After that gate, user-authorized worktrees/PRs may isolate cohesive feature blocks. Follow the assigned checkout exclusively; use `codex/` branches, reviewed common contracts, disjoint writes and centrally coordinated integration. Independent PR success is not proof the integrated application passes. Do not move existing active assignments or their uncommitted changes implicitly.
- For future coordinator launches, the user requests non-interactive tool permissions: Claude and AGY use `--dangerously-skip-permissions`; this OpenCode version uses `--auto` (explicit denies remain enforced). Apply per invocation, not global settings. This does not expand task ownership or authorize destructive/out-of-scope operations.
- Use `apply_patch` for file edits when available. Never include secrets or raw provider transcripts in tracked files.
- Preserve upstream copyright/license notices for reused code. Do not copy old cloud credentials, telemetry destinations, update feeds or private artifacts.
- Future architecture: Rust core/service/CLI with versioned contracts; Electron renderer isolated from execution. Reuse the established Orca visual system. All features require real tests, not model assertions.
- Preserve SSH host ownership, folder workspaces and remote mixed-version compatibility. Loss of contact means `unverifiable`, never proof of process exit. Use `live` / `unverifiable` / `exited` for liveness verdicts.
- Use domain-specific filenames, concise non-obvious comments, platform-appropriate paths/shortcuts, and no max-lines lint overrides. Follow the prior source's applicable design and execution-boundary references before migrating those subsystems.
- Mentu recipes, when created or edited, require the installed official writing-recipes skill and successful `mentu-recipes check` and `mentu-recipes doctor --strict`. Raw run records are not proof of formal Commitment Protocol records.
- After audit acceptance, assess Mentu capability gaps during implementation, including preservation of existing integrations. Report reproducible gaps to the coordinator; do not silently reduce requirements or select a local workaround. The coordinator owns narrowly scoped upstream PRs with English rationale, regression tests and recorded evidence; discuss material scope expansions with the user. Build and verify pinned local PR revisions without waiting for upstream merge. Workers may prepare assigned changes but must not publish PRs without an explicit scoped assignment.
- Keep the latest verified preview installed for the user after each accepted implementation block. Record the installed source revision, test the packaged app, preserve previous builds and user data, and never force-close user sessions or replace a live service merely to update a binary. Installation is part of the handoff, not an optional later step.
