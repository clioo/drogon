# E5 publication boundary — independent root review

Root accepts the **finite source/provenance characterization**, not publication rights, the proposed service design, full E5 closure or product parity. Source pin: `c97906287bb7a390b25e2025b600d9fb3c25d9c3`. The frozen worker report and exact verification are linked in the companion JSON.

Independent verification matched177 unique source files against that Git revision,66 source records/83 ranges, five explicitly untracked installed dependency declarations, eight input-document hashes and all135 existing manifest entries. All12 recording roles and14 reused original assertion associations join their source records. The complete9037-file/46-package test allocation is unchanged. The verifier follows the existing source-review pattern; it does not import product code, authenticate package tarballs or evaluate rights.

Root read the worker report, all six service proposals and26 proposed cases, then sampled actual upload/delete, bundle, feedback, vendor/checker, preview, packaging and private-service-boundary bodies. The samples distinguish existing behavior from intended additions:

- The original upload client checks host including port and prevents HTTPS downgrade, but permits local HTTP development. It does not validate token expiry. Upload accepts any nonempty ticket string; deletion later requires16–64 base64url characters and sends no auth header. The proposed43-character deletion capability is new service design, not existing backend evidence.
- Anonymous feedback nulls contact fields; nonanonymous supplied identity is not authenticated. New server Unicode/transport caps and telemetry batch protocol are proposals, not audited original server behavior.
- Feature-wall metadata records source-file Git author time, not verified recording time. The source checker checks36 names and11MiB total, not image contents, useful motion, privacy or rights.
- Source-tree notices and package pruning selectors do not prove notices ship in final renderer bundles. Installed Lucide/KaTeX declarations remain separately qualified; desktop Lucide does not establish the docs version's provenance.
- The checked-in cloud README explicitly places API/auth in a private repository. Audited relay services do not establish behavior of those missing backends.

The unchanged original feature-wall checker ran successfully under Node24.19.0 and26.0.0: `Feature wall assets: 10.92 MB / 11.00 MB`. This is **one standalone read-only integrity check repeated on two runtimes**, not new Vitest cases or a rendered/candidate test. No assets were decoded, copied, generated or published; no backend was contacted or provisioned.

## Remaining decisions and gates

AO-RIGHTS-01 retains individual authorization unknowns. AO-NOTICES-01 retains authenticated component provenance and final package inspection. AO-RECORDINGS-01 retains original capture/content authorization or an explicitly accepted independent replacement. AO-SERVICE-01 retains unavailable original backend source and proposal-only ownership, region, retention and consent decisions. These are not silently converted into ordinary execution debt or cleared by this review. Do not change named bot characters/provider identities or provision new services on the strength of this document.

All existing local-source acceptances remain in force; historical residual wording in inherited records does not reopen accepted work. E3 integrations and the two bounded parser/history/runtime joins remain the next review work. Audit remains10/12=83.3%, delta0, medium-low confidence; the flexible24-hour full-fidelity target remains high risk, without a defensible ETA. Actual Sol implementation starts only after root accepts the complete source-audit gate.

Orca skill lifecycle was preserved: publication Task was already delivered/released with no new assignment, and both remaining audit Dispatches were independently observed live. Mentu Navigator's setup check succeeded; consequential conclusions here come from the actual named source bodies, not navigation summaries.
