# E3 runtime source acceptance

2026-09-06, after2c682b0. Root accepts the assigned16 runtime result groups /211 exact methods in `audit-closure/e3-bridge/result-runtime/report.md` and `contracts.json`, qualified below. This closes their five named local joins and the additional WSL transcript-process chain at the documented immediate ownership boundaries. It does not accept all E3, the three independently assigned updater/worktree/file services, a recursive import certification or product parity.

## Evidence and scope

Root read the runtime and additional-service contract narratives, all39 method-specific browser result/correction summaries and the explicitly reused51-method raw translator contract. The211-row table was checked against the exact assigned ID/method set; reused result contracts preserve receiver-specific envelopes rather than inventing generic success types.

Independent verification matched701 file/range records against both the current source reference and pinned `c97906287bb7a390b25e2025b600d9fb3c25d9c3`:382 parent source/assertion records,211 receiver records and108 leaf anchors covering90 browser methods,408 distinct files. All73 contract IDs and method source/contract references resolve. Nine input-document bindings match. All13 original allocation pointers/counts and ordered-array hashes match using recursively sorted JSON keys; compact JSON without canonical key ordering is not the same hash algorithm.70 execution entries,33 original test records and47 inherited acceptance entries are unchanged, including every10 BM/14 R/12 F gate and the11 domain gates. The full46-package/9037-file allocation remains mandatory; overlapping subsets are not summed as new coverage.

Root independently sampled the actual local reattach body and stable-owner comparison, complete structured exit helper and its production caller, complete native subprocess wrapper, Session write/resize/liveness slice, daemon probe/cache slice, complete raw browser result translator and cookie-clear transaction, and the CDP cookie write/restore slice. Seven expanded original exit-proof assertions were read, not executed. Exact paths/ranges and frozen artifact hashes are in the JSON companion. This is independent sampling, not a claim root reread all408 files or the complete child report.

## Binding corrections and tests

- **RRT-D01:** `reattachLocalPty` returns id/pid/reattach/optional WSL without incarnation. Stable-owner checks reject missing or different expected incarnation. This source composition needs a real route fixture, not a claim an incident was reproduced. Candidate recovery must retain exact identity and never spawn over an unverifiable owner.
- **RRT-D02:** the production structured TUI wait loops only while the PTY record says connected; absent or disconnected ends successfully. The exit helper trusts that success without calling its process probe. The original first test explicitly uses a successful empty wait stub and asserts no probe. That proves the helper's current contract, not host-authoritative death. Ratify a discriminating source characterization and corrected takeover gate retaining `live / unverifiable / exited`; no user process was queried or signalled.
- **RRT-D03:** native write/resize exceptions mark a private wrapper flag dead and are swallowed; Session liveness remains state-based and resize updates emulator before native. Later wrapper kill can no-op while physical exit is still unproved. Preserve byte admission, emulator state, actual effect and process proof as distinct outcomes, with corrected recovery tests rather than fake success.
- **RRT-D04 naming correction:** `result.size !== null` belongs to **probePtyLiveness**, not **hasPty**. The latter checks the local active-session set. A missing size property can make the probe return true, while transport/other errors return null and only an unsupported-request response activates legacy inventory fallback. This correction supersedes the report's shorthand and identifies the real fixture target.
- **RRT-J05 lock correction:** `withCookieMutationLock` keys the actual object passed. It serializes a real Session when the caller passes one; an importer using a fresh adapter does not share that inner lock. The importer's outer real-Session lock must span clear/write/rollback. Do not infer universal per-partition locking from the inner helper. Frozen removal coordinates, complete snapshot coverage, partition-family exclusions and sequential same-coordinate removal survive; rollback can fail after real mutations. A malformed CDP set-cookie result is accepted unless success is explicitly false, so wrapper success is not universal native write proof.
- **Browser correction precedence:** the parent result/correction fields supersede retained leaf shorthand. Mouse click can issue optional evaluation plus press/release (three commands, not a blanket two-roundtrip claim); screencast unsubscribe cleanup can stop CDP despite its old boundary label. Raw translator success returns unvalidated data; null or malformed error values can escape the JSON parse catch. Operations can take effect before parse/timeout/error, and synthetic no-op receipts are not implementation evidence.

These are ratified characterization/correction test obligations, not permission to delete supported workflows or change user-facing behavior arbitrarily. Each feature wave still needs its full original baseline and assertion-preserving port, meaningful candidate behavioral RED, implementation GREEN and integrated OS/SSH/folder/mixed-version/rendered proof. Source and candidate failures remain separate. No test, model inference, browser cookie operation, installation or product mutation ran in this review.

## Remaining owner joins and lifecycle

The integrations assignment still owns configured updater status, managed worktree creation results, and common routed file read/write/commit results. Runtime caller projections are accepted here; those owning results are not silently borrowed from types. Root coordinates their final join, so leads should deliver finite owned evidence rather than wait cyclically for other areas' global acceptance.

Runtime Task `task_c8c718dc586c` / Dispatch `ctx_73644d3d0ebf` reported succeeded (`msg_90e2560ecddb`). Root released it immediately before acknowledgement: receipt `db33f3b6-d2f4-4a95-ab69-ac6fe75c28a3`, retained/external_terminal/processAction none. The child's settled release is lead-reported and retained in the frozen evidence, not root-replayed. A late attempt to send the cookie qualification was rejected as dispatch_inactive; the correction is recorded here rather than pretending the worker received it or reopening the settled task.

Audit remains10/12=83.3%, delta0, medium-low confidence, no defensible ETA and high flexible24-hour risk. Three Astra audits remain active after this handoff. Actual Sol leads and approved workers begin implementation only after the complete source audit; no live Astra is relabeled Sol. No audit-only preview update is necessary, and all existing product changes remain untouched.
