# Root acceptance of bounded audit follow-ups

2026-09-06. Source `c97906287bb7a390b25e2025b600d9fb3c25d9c3`. These decisions concern **source characterization**, not executed product parity. E1, E2 and E4 source-audit reconciliations are accepted at their final checkpoints below; E3/E5 remain open. Root review follows the agreed completeness-plus-independent-source-sampling method, not a claim to independently reread every line the lead read. Earlier pending statements describe their checkpoint and are superseded only by explicit later acceptance.

## E1 final source gate accepted

Root accepts `audit-closure/e1-ui/followup-final-source-gaps.md/json` with the qualifications below. Its22 final source contracts reconcile all152 ordered initial obligations (70 remaining,78 inherited and4 enumeration entries) using actual source bodies and prior accepted caller/web/settings contracts. All50 feature identities,42 complete original cards,8 supplemental records,19 original invariant records,104 original test records and16 full allocation references remain intact. The compact caller invariant references and expanded final records use different schemas; root separately verified identity, expectation and retained tests against the web records. All15 inherited web-test obligations are preserved. Earlier OPEN text inside frozen cards remains provenance, not a reason to erase it or reopen already resolved source work.

Root read all22 contract narratives and their acceptance/correction boundaries. Independent completeness verification now covers342 distinct source/assertion/Drogon-delta file hashes against current bytes and pinned Git blobs (expanding the earlier296-file check to inherited card/invariant/supplemental evidence),169 inclusive physical-line hashes and20 input-document bindings. Nineteen inputs match current bytes; the root review input exactly matches `edfb26af92f6ac363690ad1c4306d474ae97a143`, SHA256 `e6cd1ac0189e45dd6ae844835a393bbb1536de4e1cf099dc5d4b0ad0c295312d`. Contract IDs and every declared source/assertion reference resolve. An initial verifier used object-property traversal order for obligations; the corrected check uses the report's explicit per-card remaining/inherited ordering and verifies all152 exact pointer/text pairs. This was verifier ordering, not lost evidence.

Accepted fingerprints: Markdown `31ad732b1555eac432257b85c724f775b56b9184d4f532dba96d26b8b868cb61`; JSON `29ce969643b5eb223fa7352da36a58d170846e40742cf7cb290c8614533b329a`; native-chat/automation leaf JSON `2f2af5684b7f3ea735f329f44a988ac5427fd7883ffe5f9b4a215597fad00331`. Builders are archived provenance, not executed or endorsed migration programs.

Independent source samples fully read project-group-owner-routing, FloatingTerminalResizeHandles, retained-run-reconciliation, native-chat PTY/structured send hooks and file-attachment actions, use-mentu-session-loading and diagnostic-bundle-upload; additional worktree-runtime-owner-index:285–318 and use-header-drag:65–110,150–182. These are representative checks, not a root reread of every342 file or execution of every assertion.

- Duplicate project-group IDs can lose unambiguous owner routing; nullish fallback may then consult focused settings. Keep exact host/group collision tests rather than declaring a wrong-host mutation already reproduced.
- Floating resize pointer cancellation commits the geometry through the same finishing callback. PTY chat clears after issuing send; structured chat clears only on accepted submission. Attachment-picker rejection is not caught in its action hook. These need faithful source baselines and explicit correction decisions, not silently homogenized behavior.
- Automation reconciliation's periodic grace/strand handling is not proof a disconnected execution host exited. Preserve exact owner, result and recovery contracts plus the E3 backend join.
- Mentu loading keys discovery by session/target and recipe loading by session/target/path. Cleanup suppresses stale Promise application, not underlying execution; persisted drafts are reapplied to a valid document and stale run evidence resets on a changed selection. Run records still do not establish formal Commitment Protocol records. Full restore/coalescing/backend execution remains required.
- **E1-FG-22 correction:** `validateUploadUrl` allows HTTP or HTTPS, requires equal URL.host (including port), and prohibits downgrade when the configured token endpoint is HTTPS. It does not universally enforce HTTPS. Preview0600/directory0700 are creation requests, not proof of existing-directory protection or Windows ACLs. E5's accepted diagnostics qualifications govern. Preserve original source cases and separate intended hardening tests; no actual payload upload or private-data inspection was performed.

**Decision:** E1 source reconciliation closes at the named UI/caller boundaries. E3 owning-domain results and E5 deeper services/platform/asset obligations are not waived by this decision. All9037-file allocation obligations, original assertions, added behavior fixtures, screenshots and journeys, source baselines, candidate behavioral RED/GREEN and real OS/SSH/folder/version/performance proof remain T1–T4. No source test, candidate test, rendered action, install or product change ran in this E1 review.

The final root Dispatch `ctx_d78d71a450c6` completed via `msg_7ab260055982` and was officially released before acknowledgment: receipt `4ca0b6c6-4cf2-4a7d-9aaf-4562325b2f48`, retained/external_terminal/processAction none. The child's release `96a6133c-8e69-408c-bf93-6622e4f86987` remains lead-reported, not an independent root replay. Stable source-audit index advances9/12→10/12 (83.3%, medium-low confidence), not product completion. E3/E5 still gate actual Sol implementation launches.

## E5 configuration/CI and infra entry-source handoff accepted

Root accepts `audit-closure/e5-platform/config-ci-final.md/json` at its finite configuration/workflow/infra-entry boundary, with the qualifications below; this is not full E5 closure. The full712-line parent report was read, including all52 workflow accounts,28 configuration contracts,13 runner entries,14 leaf-derived infra entries and9 operational entries, corrections and unrun acceptance matrices. The lead's corrected infra synthesis is the reviewed leaf interface; root does not claim an independent reread of its entire655-line leaf report.

Independent checks match all22 ordered O-CONFIG-CI paths and18 O-INFRA-OPS paths, all58 pinned workflow paths (52 new +6 reused, unique, no extra/missing path), all15 WP-SUP-CONFIG path/hash allocations,172 current file hashes,221 inclusive ranges (117 source +104 job ranges), and ten unchanged input-document fingerprints. **168 files also match pinned Git blobs; four .tsbuildinfo caches are untracked generated metadata, not pinned source.** The first all-pinned verifier correctly refused such a cache; the corrected classification admits only the four explicitly labeled generated cache rows and does not count them as behavior or test proof. Do not reproduce those caches as authoritative source during the rewrite.

Artifact SHA256: Markdown `b66ec93ce4a60c300f265839f326a6047eb45dc2647bdc7f50b977f62d62e10c`; JSON `c0d824acbbce6bf4e7747dcf88ee78881e651a11726fbde0bd7b7b82798c1f2b`; infra leaf Markdown `1171a5441b4dc10d0b64bfd0d1eaebc0750bd961743dc43fc8ef8e2d48f462d6`, JSON `76d405fcbbfe13b6353b4e92cefaff8d9e5a08d35879caf9dfb10467ac9687c4`. Source counts include metadata and reused evidence, not172 independently read behavioral implementations.

Root independently read full cloud-monitor-relay-clock-skew.yml, gcloud-client.ts/test, dashboard-snapshot.ts/test, check-reliability-gates.mjs, run-cli-case.sh and relay-gce-startup.sh.tftpl. Additional samples: incident-monitor-cli.ts:249–303,360–493 and its test:395–454; targeted production.tfvars declaration enumeration confirmed exactly29 named cells. No workflow, startup, monitoring, Docker, cloud CLI or Terraform operation was executed.

- Clock-skew loop covers c1–c22, while the pinned topology declares c1–c29. This is a source coverage mismatch, not evidence the unmonitored cells are unhealthy or deployed. Preserve an original characterization and separate intended dynamically bounded monitoring regression.
- Monitor segmentation catches its private stop symbol and returns0 while persisted completedAt remains null. The original assertion explicitly tests this then a resumed completed window. Successful segment exit must never alone authorize migration. State files use chmod, file fsync and rename; no directory fsync, whole-operation atomicity or cross-process exclusion is proved.
- Dashboard missing latest metrics become summary zero; last-good data is keyed by environment and may satisfy a different requested time window with stale:true. Cache tests inject the builder and cannot validate real metric/inventory assembly or cloud authorization. Unknown capacity remains distinct from zero and execution-host process verdicts.
- **Gcloud assertion qualification:** the access-token test checks equal values, refresh count and cache reuse, not exact command arguments. The identity-token test checks the audience/include-email array. Five-minute cache TTL is not token expiry/signature verification; identity requests do not share a pending Promise. The two unchanged source tests passed separately under the isolated admitted baseline described in cloud-gcloud-source-baseline.md/json, not as execution of the entire configuration report.
- Reliability checker validates declared metadata and cross-field consistency, not actual run truth, assertion bodies, numeric soak history or commands. Its source acceptance cannot certify the claims inside reliability-gates.jsonc.
- **CFG23 qualification:** the CLI fixture returns wrapper0 after printing RESULT status for ordinary failures, including version mismatch93. Timeout124 maps to wrapper94; signal-like status>=128 maps to92. The host must inspect embedded verdicts. Script exit alone is not CLI behavior acceptance.
- Startup cleanup removes temporary env and Docker configuration paths; it does not erase data retained in container configuration. Pull/start/SQL-readiness/logging failures can leave partial state. Terraform declarations and role names are not deployed access-denial proof or permission to transfer upstream identities/resources.

SEAM-CLOUD, SEAM-PATCH, SEAM-LOCALIZATION and SEAM-GENERAL-SCRIPTS retain their stated owning source/implementation boundaries and all original suites. Entry-source acceptance is not a claim that all454 scripts or patch internals were read; unresolved local code is not silently reclassified as an external dependency. Full E5 source reconciliation must account for its service/platform joins; all fixture, publication, real native/OS/SSH, identity, rollback, selector/exclusion and baseline/port/candidate RED-GREEN gates survive. No required capability is removed.

The configuration root Dispatch was officially released before delivery acknowledgment (receipt `838f83cb-ef52-452f-bbc3-8da939df4cd8`, retained/external_terminal/processAction none) and its same Astra process now owns a separate E3 result Task. The previous leaf's read-only Git usage violated its explicit no-Git assignment; that documented deviation and the corrected no-tests claim remain visible, not approved retroactively. Stable audit stays10/12 (83.3%, medium-low confidence); full E3/E5 and actual Sol implementation remain pending.

## E5 remaining-platform12 contracts: bounded acceptance, area still open

Root read all548 lines of `audit-closure/e5-platform/followup-remaining-platform.md`, including every group, entry, omission, assertion association and future-command limit. The20 ordered G IDs match initial `closure.json`; all20 inherited still-open obligation arrays and complete test-allocation pointer sets remain unchanged. The21 packaging IDs match `parity-build-distribution-entrypoints.json` exactly. The12 new IDs are RP-U1/U2/U3/W1/R1/M1/M2/C1/C2/C3/C4/A1, not12 newly closed audit groups.

Independent provenance verification:132 file records (99 source,21 assertion,12 leaf) deduplicate to128 paths; every hash matches both current reference bytes and exact pinned Git blobs. All141 byte-preserving inclusive ranges match. Of30 reused documents,29 match current bytes; `audit-root-followup-review.md` matches its exact7816505 version, before root's later E4/E3 acceptance, so this is explained historical-document drift. The21 assertion files exist, but none was executed in this review. Artifact SHA256: Markdown `45d4cd5a3e7ee6e98987696bd46a680219bc39a7d8aed465c1b0891c26b9fb28`; JSON `5d6225e41d6aebb0d1656a24e2dcf8d451346a473a611180f05ba3dc904d6f14`.

Root independently read complete source bodies for updater-install-execution, main-process-serve, ui-slice-trust-actions, wsl-running-distro-cache, wsl-hook-relay-reattach, mobile-relay-pairing-journal-store and the cloud fence broker's app, mutation-lease, google-metadata and mutation-lease.test. Additional source intervals: `src/main/ssh/ssh-relay-session.ts:2180–2255` and `src/main/ipc/ssh-pty-output-intake-registry.ts:120–151`. These are representative semantic checks, not a claim that root independently read all128 full files.

Confirmed boundaries requiring faithful migration and separate correction fixtures:

- Native update invocation precedes local PTY killing and close-listener removal; failed supervised handoff persistence returns before those actions. Post-commit cleanup failure intentionally preserves quitting/handoff state. Native invocation and an IPC-ready message do not prove replacement-process or PTY survival.
- Trust changes optimistically update the renderer and log rejected `ui.set` writes without rollback. Pairing-journal metadata precedes secret storage under a module-local Promise chain; stale identity checks do not make metadata/keychain publication cross-process atomic.
- Running-distro failures retain cached information, while successful empty results replace it; synchronous probe throws occur before the promise catch. Proven local reattach derives its distro from the result, not current preference or SSH ownership.
- Normal SSH delivery retirement ignores the boolean from `applySshPtySourceCancellationProof`; missing intake returns false but retirement can still proceed. The recovery-proof function is different. This is a source defect to characterize, not evidence that an unavailable host exited.
- Fence broker has no in-app bearer guard; deployment IAM declarations are not proof of actual access restriction. Its lease uses generation-conditional writes and a35-minute TTL, but separate metadata/media reads are not a consistent snapshot. Identical request digests can replace an unexpired lease; no renewal or child deadline is owned here. Operation failure preserves the lease, and release failure may report failure after a successful operation. The five original lease assertions use injected fetch mocks; the last case varies the expected generation, not every field as its broad title might imply.

**Disposition:** accept these12 finite source contracts and the explicit owner/omission map. E5 remains open for O-CONFIG-CI, O-CLOUD-STATE, O-INFRA-OPS, O-RELAY, O-MOBILE, O-ASSETS-OBS and actual cross-area source dependencies. The three independent residual assignments in audit-closure-coordination.md resolve those gaps without changing the12-group denominator. General config/workflow/asset/relay implementation is not certified by selection rules, class names or registration lists. Preserve all T-LOCAL/MOBILE/CLOUD/OPS commands, full source-test allocation and real OS/SSH/native/cloud/Electron acceptance. Cloud lockfile pins Vitest4.1.9; only desktop-root4.1.11 is currently installed at the inspected paths, so do not silently call a4.1.11 cloud run the exact locked baseline. No tests, installs or cloud operations ran here.

## E3 final615/96 boundary map: bounded acceptance, area still open

Root read the complete `audit-closure/e3-bridge/followup-final-boundaries.md`, including its88 assertion-body associations and all BM/R/F execution obligations. Independently verified356 declared source-file hashes against both current reference bytes and exact pinned Git blobs,14 frozen input hashes, and88 callback-body hashes. The latter hash the callback block from its opening to closing brace, not the full test-declaration line interval; an initial line-interval comparison therefore did not match and was superseded by this exact callback-body check. No mismatch remains in these checks. The96 push IDs/channels exactly match `bridge-map.json`; all615 names, receiver files, declaration lines and group names exactly match `parity-source-bridges.json`, with no duplicate name or missing row. These are integrity/completeness checks, not615 executed receiver tests.

Reviewed artifact fingerprints: Markdown `37dd8ae333650d36535a84bdb2ce75336e7ab77379ce8b042235c39dd1fd1b28`; JSON `4f6a744ce71689aa6a31330cbb0f9e0b913156416fee3c1fad840673509582f1`. Independent source samples fully read `dispatcher-request-parsing.ts`, `methods/mentu.ts`, `updater-status-ipc-bridge.ts` and its original test, and `ui-slice-update-actions.ts`; additionally read `automation-dispatch-handler.ts:1–115`, `workspace-space.ts:1–150`, and the preload traffic-light call at127–128. Paths are under the pinned original's respective main runtime/RPC or renderer/preload trees.

Confirmed consequential distinctions:

- Null-schema RPC methods ignore parameters; other schemas receive `request.params ?? {}`. Mentu capability's optional WorkspaceTarget consequently does not admit omitted input at this dispatcher boundary because the required workspacePath is missing from the normalized object. Keep source characterization and intended optional-call correction distinct.
- Updater subscription starts after requesting the snapshot; a late snapshot unconditionally updates the store. The original assertion explicitly preserves this ordering/overwrite. Listener disposal alone does not cancel that Promise. The store separately resets or retains changelog by phase; do not conflate its actions with producer equality policy.
- Workspace-space progress admits a different scan ID while scanning, replaces previous measurements for that ID, and appends duplicate incoming measurements. Existing host-qualified deletion tests are not proof of strict progress admission.
- Automation result forwarding does not include a dispatch token by default; its host-published event is intentionally not duplicated by a local renderer emission. The downstream completion/state machine still needs the assigned owning-domain reconciliation.
- PMR-FAF-001 remains corrected: `syncTrafficLights` sends the scalar zoomFactor. The report's old pending45 and67% statements are superseded by d1dc550 and the current9/12 index, not reasons to repeat accepted work.

**Disposition:** accept the finite schema/handler/push-to-consumer map and declared test queue as bounded source characterization. E3 is NOT closed. S-FACTORIES and genuine uncovered domain-result/consumer contracts remain source work, now explicitly assigned in `task_89c732c9e750 / ctx_b0d85751e42b`. The new task must join actual owning contracts rather than label unknown behavior "owned elsewhere" or recursively inspect all imports. S-REMOTE-LIFECYCLE is an already identified admission race, with separate regression debt; the two legacy receivers retain unknown producer provenance and compatibility requirements without further history fishing. All original BM/R/F gates, exact whole-file baseline queues, behavioral candidate RED/GREEN and real Electron/host/platform fixtures survive. No original or candidate test ran in this review.

## E4-S1: accepted at CLI handler/request-builder boundary

Input: `audit-closure/e4-cli/followup-command-semantics.json` and its Markdown companion. Root reconciled every canonical name against the previously accepted `parity-source-contracts.json`: **234 records, 234 unique names, 204 new plus 30 reused, zero missing/extra names, zero missing literal allowed flags**. This check does not resolve inherited flag spreads by itself; the existing shared argument contract and the report's explicit shared/factory rules remain necessary. Root verified all74 declared evidence-file hashes against pinned Git blobs and current bytes, with no mismatch.

Independent source samples checked, not executed:

| Command / source sample | Root verification |
| --- | --- |
| `orchestration worker-start`, complete `src/cli/handlers/orchestration/worker-launch-handler.ts` | Model/effort capability probe precedes mutation; exact payload fields; coordinator identity resolution; non-ready receipt sets exit1; explicit timeout is payload, not direct call timeout. |
| `orchestration worker-release`, complete `worker-terminal-handlers.ts` | Only `release_unknown` adds exit1; retained/pending/already-released are valid receipt states; no unsupported `--from` field. Runtime cleanup ownership remains outside this CLI contract. |
| `automations create`, `automations.ts:172–205` | Schedule and target resolution precede create object; omission-aware sourceContext/runContext; workspace mode default; destination resolution after object validation; exact optional spread. Target resolution can itself call runtime before later field validation, so do not generalize the destination-specific ordering into zero prior RPCs. |
| `file open`, `file.ts:32–115,195–204` | Explicit empty worktree refused, remote omitted selector refused, absolute paths require worktree lookup; only contained paths relativized, root path refused, outside path reaches runtime unchanged. |
| `computer click`, `computer.ts:88–101` plus complete `computer-action-flags.ts` | Required app, window exclusivity before action validation, element/coordinate and modifier rules, preserved empty modifier string, actual target/action/observe payload spread. Shared key grammar remains S2. |
| `claude-teams`, `core.ts:1–90` | Exact raw teammate-mode detection, environment sanitization, Windows refusal, pane requirement, launch preparation then inherited stdio child, numeric exit propagation. Parser bypass remains separately characterized by the accepted entrypoint contract. |

Root also checked the cited worker-start assertion body at `orchestration-worker-cli.test.ts:145–165`: it mocks an outcome_unknown receipt and asserts exit1. Its broader test title is not proof that every receipt variant executes. Per-command future tests remain obligations, not existing coverage.

**Disposition:** S1 finite mapping accepted as source characterization, with S2 shared validators, S3 transport, S4 local effects and S5 formatter boundaries still explicit. No all-command baseline, candidate equivalence, process safety or publication authority is inferred. E4 remains open until its remaining source contracts are reconciled; T1–T4 execution obligations survive that closure.

## E5 release/build/shim entrypoints: accepted bounded characterization

Input: `audit-closure/e5-platform/followup-release-entrypoints.md/json`. Root read the complete report across bounded sections and checked all64 declared source fingerprints against pinned Git blobs and working bytes, no mismatch. Source sampling fully read:

- `config/scripts/publish-complete-draft-releases.mjs` and `verify-release-required-assets.mjs`.
- `config/scripts/dev-cli-terminal-wrapper.mjs`, `src/main/cli/cli-dev-launcher.ts`, `cli-install-path-format.ts`.
- Workflow branches at `.github/workflows/release-cut.yml:318–341,1877–1880,1924–1999`.

Confirmed: early recovery directly publishes bot plain-RC drafts after ancestry and asset metadata, without prior workflow/signature/digest checks; the verifier admits missing size/state and rejects exactly zero size/non-uploaded truthy state. This is a source control-flow fact, not a live release operation or test simulation. Windows inner signature verification is explicitly warn-only (`ORCA_WINDOWS_INNER_SIGNATURE_REQUIRED: 'false'`); outer and inner checks must not be conflated.

Confirmed distinct shell-escaping contracts: terminal wrappers double percent signs on Windows but interpolate POSIX values using JSON double quotes; installer launchers use safe POSIX single quotes but only double Windows quote characters. Neither implementation proves arbitrary-path correctness across both shells. No shell containing user-controlled input was executed by this review.

**Disposition:** accept the finite R1–R4/B1–B2/L1–L2 source-entrypoint characterization with all stated downstream boundaries and unrun assertions intact. No whole-directory, platform, signing, installation or build acceptance. G7 U1–U3, remaining config/build/cloud/assets/infra and named O3–O5 helper semantics remain open and assigned/separate.

## E2 field reconciliation and explicit source exceptions

Root accepts the bounded214-field source routing reconciliation in `audit-closure/e2-settings/closure.json`, not the complete E2 gate. Names are unique, match canonical declaration order exactly, and every row has a contract, source anchor and remaining acceptance obligation. All271 source-file hashes match both pinned Git blobs and working bytes. Independent samples confirmed startup's actual legacy keybinding getter (before file-existence migration checks), dedicated cached file authority, temp-write/rename without fsync, Claude teams mode launch consumption, PTY history isolation defaulting true and sidebar migration preferring an explicit UI boolean.

The three exceptions remain **preserved contracts**, not missing rows or authorization to remove settings:

- **E2-O02 / artifactsEnabled:** declaration explicitly deprecated; default true. Strict RPC `SettingsUpdate` does not admit it; the located five-case artifact capability test distinguishes it from `artifactSharingEnabled`, including fail-closed reads. Root read that test but did not execute it. Preserve legacy data and current artifact availability/publish-consent/visibility as distinct concepts.
- **E2-O02 / experimentalMobile:** default false and telemetry whitelist membership verified. Keep as legacy/metadata in this bounded source characterization, not a proven live feature gate or an alias for default-true `mobileEmulatorEnabled` or `showMobileButton`. Targeted production-name search also found test-support fixtures; no claim of a universal absence of dynamic consumers or permission to drop data.
- **E2-O01 / markdownReviewToolsEnabled:** Settings toggle persists independently from the traced standard/floating editor annotation props. Root traced `useRichMarkdownReviewData` into controller creation guard and review-rail positioning. Standard editor defaults true, floating explicitly false, RichMarkdownEditor default false; canAnnotate requires the prop and a relative source path. Rail visibility (existing notes plus open state) is distinct from creation/positioning permissions. Accept this explicit source mismatch, not “no effect anywhere” or a rendered toggle test. Preserve the advertised capability and require an explicit source-characterization/intended-fix regression during migration.

Sources independently reviewed in this follow-up include `src/shared/default-global-settings.ts:139,215`, `global-settings-types.ts:229–236,403–405`, `telemetry-property-schemas.ts:179–205`, complete `src/main/runtime/rpc/methods/client-settings-schemas.ts` and `artifact-sharing-capability-grant.test.ts`, startup/account/keybinding source neighborhoods, and editor `useRichMarkdownReviewData.ts` plus controller/rail guards. Prior full prop-chain evidence remains in `audit-semantic-followups.md`.

E2-O01/O02 source dispositions accepted; live true/false behavior, persistence, host/OS and candidate correction proofs stay T1–T4 debt. E2-O03's88-action semantics and SK boundary acceptance are still required before E2 closure. Root sent this disposition to the current E2 Dispatch in `msg_71e1f96369f5`; no duplicate scan or leaf is requested.

### E2 SK01–SK16 source boundary acceptance

Root accepts the16-row shared shortcut-boundary characterization in `audit-closure/e2-settings/shortcut-contracts.tsv` together with its explicit future acceptance obligations. All271 evidence hashes were already independently verified. Additional source sampling fully read definitions, normalization, matching-key, plugin-command-keybindings, plugin alias identities and Option-as-Alt detection/hook; read actual conflict grouping, renderer capture/plugin/blur order, native recorder/paste/dictation/held-switcher ownership and symbolic-hotkey parameter/layout conversion. Prior keybinding file/service/migration review covers the persistence-specific rows.

Concrete distinctions survive acceptance: dynamic plugin keys are syntax-validated and bounded to400 characters, not a finite catalog; aliasable built-in actions are a different closed set. Matching uses exact modifier equality and conditional logical/physical fallback, not universal AltGr immunity. App-focus plugin precedence is guarded by earlier editable/floating handling. The Option-as-Alt result is based on the implemented nine-key fingerprint, not proof of an OS layout name; explicit sided overrides bypass the default. File/API null semantics, cached snapshots, publication and actual renderer effects remain separate. No real keyboard layout was changed/probed and no Electron keyboard journey ran in this review.

This accepts the shared source boundaries, not E2-O03's pending88 action-body map or source/candidate/platform execution. The original93-file test queue and complete source-suite obligations are preserved. E2 remains open until the final action map is independently reconciled.

## E3 Bots/Mentu21: accepted bounded source characterization

Root verified all78 declared source hashes against pinned Git blobs and working bytes. The21 unique callback rows contain input/result descriptions, source anchors and explicit missing-acceptance obligations. Full original Bots and Mentu registration modules independently confirm the7+14 identity set, trusted-renderer guard, outer cast-only argument forwarding, Mentu remove/re-register and Bots direct registration. Typed Mentu arguments are not runtime validation.

Independent source sampling read complete `src/main/ipc/bot-schemas.ts`, `bot-schemas.test.ts`, `src/main/persistence/loading-store/write-flush-barriers.ts`, `src/main/mentu/mentu-session-execution.ts`, `mentu-session-recovery.ts` and `mentu-run-evidence.ts`:

- Bot schema really permits only null explicit models/session models and omits recipe in the strict responsibility object. The existing three tests cover bounded default creation, unsupported/undeclared inputs and mixed trigger refusal, not nonnull model or recipe support through actual registered IPC. Do not weaken intended capabilities to reproduce this mismatch.
- The flush wrapper catches primary and active-view errors and returns early after finalization; a successful in-memory Bot write cannot be reported as durable merely because it calls flush. Actual durable primitive evidence remains separate.
- Review/approval/completed/in-flight/controller state lives in Maps. Cancel requests an abort; it is not descendant exit proof. Completed-result caching and concurrent request coalescing are not cross-process restart guarantees.
- Recovery digest binds run/recipe and step label/backend/model, not outcomes, variables, attempts or full optional state. Absent recipe_ref is admitted; retry label may come from optional state. Preserve these as explicit gaps to test/fix, not comprehensive recovery identity.
- Evidence reader derives live/exited from stored status, verifies the primary run ID but not every optional record identity, and overwrites duplicate output labels. No process probe or formal Commitment Protocol record exists in that path. Migration must distinguish stored outcome from real host observation.

**Disposition:** accept the21 callback source/payload/state map in `audit-closure/e3-bridge/followup-bots-mentu.md/json` with its BM-AUTH through BM-EVIDENCE obligations unchanged. This is neither native Mentu-internal review nor proof of actual callback/runner/renderer execution. E3's remaining45 callbacks, RPC/push consumers and explicitly unknown legacy producers remain separate. No group percentage increase or product implementation follows from this scoped acceptance.

## E1 web projection and navigation identities

Root accepts the bounded web-factory/source-navigation characterization in `audit-closure/e1-ui/followup-web-navigation.md/json`, with the13 inherited caller chains still open. All158 declared evidence hashes independently match pinned Git blobs and working bytes; all50 production modules under `web/preload-api` have evidence entries and the report contains50 unique named surfaces. These checks verify completeness/provenance, not the truth of every semantic row. Independent navigation sampling fully read `settings-navigation-types.ts`, `settings-navigation-foundations.ts`, `ui-slice-settings-actions.ts`, `settings-deep-link-target-watcher.ts` and `top-level-view.ts`, plus the page-effect host/intent ordering, Activity actions, hydration sanitizer and Sidebar bell handler. Previous root web-fallback probes and Activity assertions retain their narrower evidence scope.

Root ratifies these **acceptance identities**, not new features or extra closed audit groups:

- Keep `PUI-SETTINGS-000` taxonomy, `PUI-SETTINGS-002` control search and `PUI-SETTINGS-003` navigation/deep links. `PUI-SETTINGS-001` remains a historical aggregate alias, not the first member of a generated numeric range. The old `001..035` shorthand must not overwrite search/navigation or create fictional numeric cards.
- For each of the35 exact literals in pinned `SETTINGS_NAV_TARGETS`, use `PUI-SETTINGS-PANE-<literal>`. The complete explicit mapping is the frozen follow-up JSON at `navigation.settings.fixedPanes`. This identifies targetable panes, not unconditional visibility or verified controls.
- For the three exact `SETTINGS_NAV_INTENTS`, use `PUI-SETTINGS-INTENT-<literal>`; for the five exported named subtarget constants, use `PUI-SETTINGS-SUBTARGET-<value>`. The frozen JSON lists both mappings. Arbitrary `sectionId` strings remain accepted by the source validator; five named constants are not an exhaustive runtime subtarget allowlist.
- `PUI-SETTINGS-PANE-repo` carries separate repoId/hostId and applicable setupId/representativeSectionId instance metadata. These are audit-instance fields, not a claim that all four are request fields. Source `SettingsNavigationTarget` has no setupId property; the page effect derives it from selected host data. Never manufacture acceptance IDs from user repo names or treat renderer-only host/setup selection as persisted state.
- `PUI-ACTIVITY-001` identifies legacy full-page `activeView=activity`; `PUI-SIDEBAR-003` identifies the separate `sidebarBody=agents` tab. Preserve restoration of Activity, its prior-view close behavior and portal obligations; a missing direct opener search result is not global unreachability. The bell actually changes sidebarBody, not activeView. These supplemental identities refine existing shell/activity scope and do not increase the frozen50-row reconciliation denominator or the12-group audit denominator.

Navigation targets and page opening remain separate operations. Target validation rejects unknown pane/intent/host but admits arbitrary string sections; requested host selection precedes mounting and scrolling. The observer only checks for presence on subsequent mutations, stops after five seconds and supports cancellation; its existence is not an executed timeout test. Keep all pane/host/intent/hidden-target, persistence, accessibility, viewport and actual Electron journeys as later test/implementation acceptance obligations.

E1's exact13 caller-chain follow-up is now active as `task_5f2606b1a214` / `ctx_0677ac521e0a`, reusing the completed lead's exact terminal with a verified input-accepted receipt. No source edits, feature implementation or duplicate factory census were assigned. Any other inherited source gap must retain a named owner/boundary; it cannot disappear merely because the13-row task finishes. Source-audit acceptance and execution debt remain separate gates.

## E5 O3/O4/O5: accepted18 install/build source boundaries

Root accepts the finite18-contract source characterization in `audit-closure/e5-platform/followup-install-build-boundaries.md/json`, with its imported-boundary exclusions and G7/G8 source remainder intact. All39 fingerprints matched pinned Git blobs and working bytes in the preceding checkpoint. Root read the full report across bounded ranges, independently read complete AppImage pruning/registration-lock modules and Windows environment expansion source/tests, and checked Electron install/extract/publication control flow at `config/scripts/install-electron-package-binary.mjs:53–120,144–190,355–425` plus `ensure-native-runtime.mjs:254–274`.

Confirmed consequential limits:

- Extracted executable existence is checked before publication, but package version usability is checked after publication. The rename rollback covers publication failures, not that subsequent usability failure; wrong-version archives must get an explicit pre-publication candidate regression. `process.exit(1)` inside the missing-executable branch bypasses its surrounding finally cleanup.
- Process-tree native health only requires the addon; the nearby comment claims enumeration that the body does not perform. ABI load is not a functioning process table.
- AppImage pruning protects named roots and staging within its namespace but treats failed age reads as stale; endpoint read errors can drop a protection. These are source heuristics, not proof an owner exited.
- Registration lock rejection always produces timeout/removal-advice text, regardless of actual error/elapsed time. Its existing three mocked tests cover option bounds, ELOCKED wording and release after operation failure, not real elapsed deadlines, live-owner safety or release-error masking. No lock or installer was executed during this review.

The earlier report's statement that release-entrypoint review was unaccepted is stale; the accepted bounded disposition above governs. No further transitive library review or real OS/package/signing success is inferred. Runtime verification remains mandatory before changing an installed preview.

Root separately executed all **four unchanged original Windows environment-expansion cases** in an isolated pinned capsule, all passed with no failed/pending/todo cases. They exercise explicit Windows/Linux branch values and fixture objects on macOS arm64, not a Windows shell/registry or personal PATH. Reproduction, source/result hashes and limits: `windows-environment-baseline-review.md`. This supersedes I07's unrun status only; the other18 test files and all real platform obligations retain their prior evidence state. E5 remains open and the audit percentage is unchanged.

## E2 final source-audit closure: accepted

Root accepts E2-O03 in `audit-closure/e2-settings/followup-shortcut-actions.md/json`. All88 unique action IDs match the accepted catalog exactly, with no missing/extra IDs or missing eligibility/event/effect/branch/acceptance fields. Root independently checked146 complete-file fingerprints against working and pinned Git bytes and all360 inclusive-range hashes, with zero mismatches. This is provenance/completeness verification, not blanket independent reading of every body.

Independent source samples confirmed: remote hard/soft reload share `browser.reload` and preserve operation-token fencing; find-in-stream displays unavailable rather than invoking a search; close-all filters unpinned editors and queues dirty ones, not terminal/browser tabs; rich Markdown replace opens/focuses replacement mode without editing text immediately; the Monaco binding installer does not install the catalog replace hook; explorer undo/redo pops before awaiting and does not restore the popped operation on failure; pane close currently executes for non-live probe results and probe rejection. Sources include complete `fileExplorerUndoRedo.ts` and `monaco-editor-input-bindings.ts`, and the exact remote navigation/chord, bulk-close, rich-search and pane-close ranges identified in the candidate.

**Decision:** E2's214-field reconciliation, SP01–SP13, explicit O01/O02 exceptions, SK01–SK16 and final88 action source contracts now satisfy this group's enumeration/routing gate. This does not accept their runtime behavior as complete or correct. Keep all47 assertion associations (covering56 rows) distinct from32 rows without associated reviewed assertions; neither number is executed coverage or proof of test absence.

SA-U01–SA-U07 remain binding implementation/test obligations. Monaco's default/custom/unbound replace behavior is an external-library integration question that T1 must establish before its feature wave; the known source registration boundary is accepted, not Monaco capability or permission to omit replacement. Preserve local/remote/preview/floating variant contracts and real event-order tests. For the source pane-close defect, `unverifiable` or a rejected probe must not be relabeled `exited` or silently authorize destructive cleanup; preserve explicit user intent/confirmation and execution-host authority while testing source characterization and intended correction separately.

All93 focused source-test paths, the complete original-suite allocation, field-specific E2-A001–A214 outcomes, source/candidate RED–GREEN and real OS/SSH/renderer acceptance survive closure under T1–T4. Source closure does not permit implementation before the other four E groups close. E2's successful worker was released through the official operation: request `7e8bcad0-23be-4603-a4cc-e5a828a982c0`, retained/external_terminal, processAction none. No personal terminal was force-closed. A late guidance send was refused with dispatch_inactive and was not retried against stale authority.

## E4 S2–S5: accepted bounded CLI helper contracts

Root accepts the22 helper contracts in `audit-closure/e4-cli/followup-cli-boundaries.md/json` at their declared caller/source boundaries. This is not full E4 closure: the frozen30 `unreviewedBoundaryPaths` are the current E4 lead's separate assignment. Root independently checked all120 distinct reviewed file hashes against both pinned Git and working bytes and all121 inclusive-range hashes (including29 assertion ranges), with zero mismatches. The assertions were not executed. Report progress of7/12 is historical; current root progress remains8/12.

Root read the22 behavioral summaries and independently sampled complete `runtime/types.ts`, `runtime/status.ts`, `orchestration-rpc-contract.ts`, `orchestration-compatibility-evidence.ts`, `windows-batch-spawn.ts`, `artifact-file-read.ts`, `runtime-bootstrap.ts`, `remote-runtime-request-response-router.ts`, `remote-runtime-client-handshake.ts`, `runtime/websocket-transport.ts` and `automation-cron-occurrence.ts`. Additional ranges: `runtime/client.ts:85–165,233–274`, `format.ts:74–200`, `managed-agent-hook-controls.ts:85–240` and `automation-schedule-parsing.ts:1–100`. Paths are under the pinned source's `src/cli`, `src/shared` or `src/main` as identified by the companion evidence. This is independent sampling plus completeness checks, not a second full read of all120 files.

Consequential decisions and clarifications:

- Successful remote RPC precedes `markEnvironmentUsed`; a local persistence exception can therefore surface after the remote operation committed and is outside the transport recovery catch. Do not infer no effects or blindly repeat a mutation. A rejected orchestration capability preflight remains cached on that client instance.
- Local status maps every process-probe exception to false. Preserve an explicit source-characterization test, but candidate ownership/liveness must distinguish `unverifiable` from `exited`; EPERM or transport loss cannot authorize cleanup. Text status is a projection, not an execution-host verdict.
- Auth JSON `null` passes JSON parsing and then reaches an unguarded `.type` access in the handshake helper. Characterize this source failure with a disposable peer and add intended typed-error cleanup tests. The CLI WebSocket adapter also drops the remote error's dedicated stage/close-code properties; code/message/data are preserved. Neither branch is a verified runtime failure result yet.
- The bootstrap selects the first matching array entry in array order; an empty modern array prevents legacy fallback. Array elements are not structurally validated by this helper. Legacy acceptance does not imply arbitrary malformed bootstrap safety.
- Schedule clarification: RRULE entries with a falsy key/value are skipped, so an empty duplicate value does not overwrite an earlier value. `BYDAY` filters empty comma tokens before validating remaining tokens; `MO,,TU` is not rejected just for its empty token, while an all-empty weekly list rejects. The report's nonempty-list wording must not be implemented as a stricter token grammar without source characterization and an explicit change decision. DOM/DOW uses OR only when both are restricted; fixed-day arithmetic is not DST correctness proof.
- Existing managed scripts refresh before presence/disabled-agent gating. No actual user-global hook file was touched in this audit. Provider-specific mutation/rollback semantics remain in the30-path assignment, not inferred from registry mocks.
- Windows batch helpers reject their explicit unsafe token set, return argument arrays and do not prove actual Windows command execution. Artifact ingestion follows opened symlinks and reads at most max+1 bytes; its helper alone is not an ownership policy. Both need the named real platform/host fixtures.
- Compatibility redaction is exact-key based; repeated object aliases become `[circular]` too. It is not general secret-string detection. JSON errors go to stdout, text errors to stderr, and `formatCliError` only returns text. Keep unknown/null/omitted fields and error precedence as tested contracts rather than silently homogenizing them.

Retain all listed original assertion bodies, extra acceptance cases, host/version matrices, real process/OS and publication gates. The corrected leaf findings are evidence inputs, not test results. The lead recorded its prior S5 child settlement and official external-terminal release; the root has not independently replayed that historical receipt and does not infer process termination from it. Current E4 work continues under a fresh Dispatch with its own14-hook child.

## E3 remaining45: accepted with an explicit payload correction

Root accepts the bounded45 callback source characterization in `audit-closure/e3-bridge/followup-remaining-callbacks.md/json`, **with the PMR-FAF-001 correction below overriding its input/behavior text**. The21 previously accepted Bots/Mentu callbacks remain separate. Root verified123 full-file hashes against pinned Git and working bytes and232 inclusive-range hashes, all matching; the45 IDs/channels exactly match the existing66-row registration map minus Bots/Mentu, and all rule/assertion/acceptance references resolve. This proves identity/integrity, not execution or blanket independent semantic coverage.

Root read all45 callback summaries, all21 shared rules, the16 defect/limitation entries,14 unrun acceptance groups and selected structural descriptions. Independent source samples included complete Skills IPC trust wrapper, cloud install IPC schemas, cloud request/auth wrappers, browser renderer trust/viewport state and main window close lifecycle. Additional samples covered the bundle request refinements (`skill-bundle-install-contract.ts:1–126`), cloud session refresh predicate and operation retry (`profile-cloud-session-refresh.ts:24–40,153–213`), cloud prepare/install/cancel route (`skill-cloud-ipc-handlers.ts:15–135,205–274`), bundle grant installer (`skill-cloud-grant-installation.ts:1–153`), update runner (`skill-update-run.ts:76–145,189–305`), TCC owner registration (`attach-main-window-services.ts:149–215`), management preview (`skill-install-management-ipc-handlers.ts:130–162`) and delete plan-to-lock boundary (`skill-delete/service.ts:49–125`). All paths refer to the pinned source and are expanded in the artifact's123-file evidence list.

Required correction and interpretation:

- **PMR-FAF-001 `ui:sync-traffic-lights` takes the supplied scalar `zoomFactor`.** Preload `ui-bridge-clipboard-and-window-controls.ts:127–128` sends it; `main-window-close-lifecycle.ts:143–145` forwards it. It is not a no-payload message and does not read the captured window's current zoom. `main-window-visual-lifecycle.ts:117–123` returns off macOS or for a destroyed window; otherwise it computes `Math.round(18 * zoomFactor - 6)` with x16, without runtime scalar validation. Preserve valid zoom behavior and characterize omitted/null/string/nonfinite inputs and actual macOS button placement under R-WINDOW. The frozen candidate's contrary wording is superseded here, not silently accepted.
- Bundle `.shape` reuse preserves field schemas but loses whole-object uniqueness/subset refinements. Later full local install parsing restores those constraints; preview has a distinct boundary. Keep tests at both admissions and every remote receiver rather than treating one successful parse as universal validation.
- `SkillCloudRequestError` is a separate class from the `OrcaCloudRequestError` required by the shared auth-retry predicate. The wrapper therefore rethrows the former rather than refreshing on its401/403. HTTP success is cast, not structurally validated;204 and invalid JSON differ. These are confirmed source paths, not a live backend reproduction.
- Explicit outer `ssh:` environment bypasses the IPC transfer cancellation map, while the grant installer routes any nonempty outer environment remotely. Cancellation instead selects the runtime-local map for `ssh:`. Keep this distinct from absent outer environment plus SSH destination; prove the route with disposable fixtures before implementation acceptance.
- Update cancellation returns immediately and can eventually expose idle without a verified descendant-exit result. Zero-exit plus rejected convergence rescan can report success. Neither UI state nor exit alone proves the required disk/host outcome. `canonicalizeSkillUpdateNames` sorts before regex validation: a singleton non-string can avoid the comparator and be regex-coerced, so malformed inputs do not all fail in the same way.
- Window callbacks do not inherit the Skills trust boundary. Only the close receipt checks sender identity in the sampled close lifecycle; TCC separately guards its live captured owner and claim. Viewport reporting has additional page ownership and finite/nonnegative metrics after its coercion stage; no `position <= maximum` check is present. Do not flatten these distinct contracts into one generic IPC claim.
- Deletion replans before acquiring per-skill locks. Cancellation choices, lock release errors and later cleanup failures do not promise whole-operation rollback. All named ownership, race, recovery and user-file preservation cases remain mandatory.

Retain all37 structural descriptions,44 assertion groups across27 unrun test files,16 defect/limitation entries and R-TRUST through R-VIEWPORT obligations. Candidate fixes need source characterization plus intended-behavior regressions, not omission of capabilities or invented stronger source guarantees. External backend policy and adversarial extraction/placement internals are explicitly bounded dependencies; their integration acceptance is still required. No actual provider, user profile, filesystem deletion, window mutation or source test ran in this review.

The full E3 gate remains open for its existing96 push-consumer rows and615-schema dynamic receiver linkage, now assigned as `task_ad78d50ed25d / ctx_0b7ead9d473d`. Unknown legacy producer provenance does not permit deleting exposed receivers. Root sent the payload correction as `msg_afc8c603c6f5`; queued guidance is not proof consumed. Audit stays8/12 (~67%), and full implementation remains gated.

## E1 thirteen inherited caller routes: bounded source acceptance

Root accepts the13 entry-to-named-boundary contracts in `audit-closure/e1-ui/followup-caller-boundaries.md/json`, not full E1 closure. Root independently verified all110 source/test file hashes against both pinned Git blobs and working bytes, all122 declared range bounds, five preserved artifact hashes, and exact13-route identity equality with the frozen web/navigation table's inherited callers. Range bounds are not range hashes or proof of reading every cited line. The17 selected assertion files are unrun here; retain the complete prior50-feature,19-invariant and full source-test requirements.

Independent source sampling includes startup-degraded-recovery and terminal-dead-session-reconcile in full, runtime-provider-accounts-client:38–310, editor-save-queue:58–165, use-checks-panel-polling in full, and main/ipc/diagnostics:1–170. All paths are in the pinned original. These support concrete distinctions: failed startup recovery preserves data and blocks unsafe saves; local session census is not authority for remote death; remote account subscriptions bypass empty local web defaults; queued saves reread drafts and preserve typing during an in-flight write; successful unchanged check signatures drive backoff, while thrown failures do not; diagnostic uploads use the original main-retained payload, not edited preview bytes. GitHub and GitLab retain separate polling routes. No original test or rendered product journey ran in this review.

Preserve the report's staged mutation, cancellation, host-switch, dirty-draft, retry and downstream service obligations. PUI-TERM-003 remains quick commands; PUI-CONN-004 remains Windows/WSL/CLI registration. Existing root Settings identity ratification governs. The report's60%/7-of-12 progress text is a stale checkpoint; authoritative progress remains8/12, approximately67%, medium-low confidence.

Full E1 remains open: original inherited source gaps for explorer actions, native chat, automations, sidebar/worktree interactions, quick commands, host attachment and other named families must be reconciled by the current finite final-source Task. Naming a service owner does not silently convert an unread source requirement to execution-only debt. Do not reopen these accepted13 routes or the accepted59 factory keys as another census.

## E4 final source gate accepted

Root accepts E4's source-reconciliation gate, combining the previously accepted234 canonical command contracts and22 S2–S5 helper contracts with all30 exact frozen paths in `audit-closure/e4-cli/followup-final-boundaries.md/json`. This closes source characterization at the documented caller/owner boundaries, not internal-algorithm certification, original test baseline, implementation, provider behavior or runtime parity.

Independent verification found zero mismatches across136 pinned/working source-and-assertion file hashes and136 inclusive-range hashes, exact ordered30-path equality, all14 frozen prior artifacts, seven unchanged original gap classes and77 existing baseline suite paths. All seven document records also reconcile: the three central-review references match historical3898738, before the root's later E1 addition; the two leaf files have no final newline and match their original byte-preserving ranges. Do not manufacture a changed source finding from those expected document/newline differences. Evidence roles preserve103 source records,29 assertion-body records and four explicitly unread assertion-pointer records; the additional20 pointer list remains preparation debt, not read bodies.

Root read the full final report and independently sampled these pinned source bodies: `src/shared/serve-update-handoff.ts`, `secure-file.ts`, `secure-path-windows-acl.ts`, `secure-path-hardening-cache.ts`, `e2ee-crypto.ts`, `child-process/run-process.ts`, and `remote-runtime-subscription-transport.ts` in full; `src/main/agent-hooks/installer-utils.ts:251–355`, `installer-utils-remote.ts:163–200,247–294`, `src/main/grok/hook-service.ts:218–302`, `src/main/codex/codex-home-paths.ts:1–94`, `src/main/startup/hydrate-shell-path.ts:160–272`, and `src/shared/secure-file.test.ts:330–364`. These are independent samples, not a claim that root reread all136 files or ran their tests.

The following decisions bind implementation and test design:

- D-ACL is confirmed in source and in the mocked original assertion: a false Windows restriction result does not stop rename publication. Preserve characterization, then require a fail-closed publication regression and real disposable Windows ACL proof. Do not copy source comments claiming stronger confidentiality than code provides.
- D-BARRIER is confirmed: the unverified deadline is armed only after termination attempts resolve. A nonsettling custom barrier can prevent that deadline; ordinary result settlement does not expose verified descendant exit. Candidate cancellation must remain bounded without falsely granting cleanup authority, and retain `live / unverifiable / exited`.
- D-PHASE is confirmed: `String(phase)` admits coercible values that evade strict phase-specific checks. Preserve source tests and require a typed-validation correction before trusting handoff readiness. Nonempty version/runtime strings and PID fields alone are not installed-binary integrity or process identity proof.
- Subscription authentication completes startup before first RPC response; callbacks can interrupt cleanup. Preserve late failure, close/open sending race and socket-budget retention tests. A broken stream never proves remote process death.
- Script and JSON writers differ: only the script path has the Windows ACL retry; JSON mode preservation is opt-in. SFTP timeouts reject without cancelling late server effects, and plain rename fallback is not guaranteed overwrite support. No blind retry or lost-contact cleanup.
- Grok synchronous removal lacks the asynchronous peer gate, while default Codex home resolution can create directories. Provider disable policies differ; keep provider-specific fixtures and avoid treating hook status as a universally read-only probe. No personal configuration was touched here.
- Shell PATH probes deliberately execute profiles and currently accumulate unbounded output; synchronous PATH restoration does not extend across returned promises. Keep the intended shell experience, with explicit resource and host-boundary regressions rather than removing profile support.

Retain all ten findings, seven gap-class reconciliations, named Codex trust/TOML/settings-promotion seams, full source suites,77 baseline commands and real OS/provider/SSH/skew fixtures. Read and run the original assertion bodies in a prepared isolated baseline before porting; then prove candidate behavioral RED before each implementation wave. Unsupported native Windows SSH hook behavior is distinct from supported WSL paths, not a license to remove existing capabilities. E3/E5 cross-area source gaps remain with their owners; global audit stays open.

Root released E4's settled Dispatch before acknowledging its completion; receipt `d2e3f383-8625-452f-b19c-7393641e8c79` is retained/external_terminal/processAction none, not a stopped-process claim. The child release is reported by the lead, not independently replayed here. No tests, product edits, profile changes, installations, pushes or PRs ran in this final review. The stable index advances from8/12 to9/12 (~75%, medium-low confidence); E1/E3/E5 still require acceptance before Sol implementation begins.

## E5 assets/observability: finite source handoff accepted, full E5 remains open

2026-09-06, after ca3a367. Root accepts the17 finite source-characterization records in `e5-platform/assets-observability-final.md/json`, with its14 inherited collector records qualified by LC-01–LC-05. This does not close full E5, establish per-asset redistribution authority, supply missing backend source or execute the130 associated suites. All12 AO remainders remain explicit obligations, not extra audit denominator units.

Root read the complete report narrative and leaf Markdown, verified307 unique current/pinned source, asset and assertion file hashes,55 inclusive read-range hashes,10 reused-document fingerprints and3 exact asset byte-equality joins. All130 test associations have the same package/domain/hash as the original allocation, and the full57 WP-CAP-DIAG paths match exactly. Existing tracked E4/E5 reports have no diff. The lead's stronger30 pre-dispatch-file preservation claim remains lead evidence where no root pre-dispatch fingerprint was available; root does not silently upgrade it. Git blob SHA1 in the leaf's mislabeled SHA256 column is superseded by the real SHA256 fields in the final companion.

Independent source samples: full `src/shared/crash-report-redaction.ts`, `src/main/observability/redactor.ts`, `src/main/ipc/diagnostics.ts`, `src/main/crash-reporting/crash-report-store.ts`, `src/main/telemetry/consent.ts`, `src/main/persistence/loading-store/loaded-cohort-migrations.ts`, and `config/scripts/vendor-feature-wall-assets.mjs`; the notice headers in `docs/site/THIRD_PARTY_NOTICES.md:1–38` and `src/renderer/src/assets/fonts/SymbolsNerdFontMono-OFL.txt:1–18`. These are independent samples, not a claim to reread every307 file or every original assertion body.

- Crash string sanitization lacks the span redactor's dedicated AWS-secret-assignment and bare-JWT rules. Structured crash detail keys do not get the span blocklist. Preserve original assertions and add synthetic differential regressions; the leaf's AKIA-prefixed example can trigger an existing rule and is not a valid general demonstration. No actual-secret test, upload or proven exploitation occurred.
- Crash store reads an unbounded JSON file and checks only that `reports` is an array before slicing5. Record/attach sanitize new values, but existing loaded fields do not pass through those methods. Promise serialization and temp/rename are not full schema validation, byte limits, private file mode or fsync durability. Do not infer those guarantees from a type assertion or a five-record cap.
- Diagnostics sends retained main-process payload, not edits in its opened preview file. It rechecks state after confirmation but has no per-bundle in-flight deduplication. A successful `shell.openPath` is not proof the user read the file; creation modes are not existing-directory or Windows ACL verification. Preserve these source cases and separately decide clearer preview UX and hardening corrections.
- Consent ignores empty CI values despite its comment, while invalid nonempty switch values can appear in warnings. Telemetry migration preserves explicit choices and defaults new missing consent to true versus existing missing consent to null. These are source policy observations, not approval to enable Drogon collection, reuse original destinations or send user telemetry.
- Font notice headers and identical Geist bytes are useful provenance, not proof every packaged asset is licensed. Feature-wall metadata uses the latest Git author timestamp, not capture time or consent. Eleven recordings point to a separate marketing checkout that was not inspected. Rights, content review and independent backend provisioning remain implementation/publication requirements; no feature or user identity is silently replaced.

The17/17 WSL rerun in ca3a367 verifies separate batch infrastructure, not these diagnostics contracts. No asset/diagnostic source test, live service, profile mutation, installation or product edit occurred in this review. Stable audit index remains9/12 (~75%, medium-low); E1/E3/E5 remain open. Subsequent Sol workers retain all baseline, port, behavioral RED/GREEN, native/host/service and installed-package obligations.

## Migration decisions and phase boundary

Preserve workflow capabilities and the actual user experience, not unsafe interpretations of source comments. Drogon's eventual publication proof must cover **every** publishing path; asset presence alone must never be described as verified signatures, compatible binaries or successful prior tests. Unknown process contact must not justify deleting/replacing owned resources. Source defects get explicit characterization and intended-behavior regression tests during implementation, not silent deletion of capabilities or claims that the source already satisfies these invariants.

Scoped acceptances do not add denominator units. E1/E2/E4 full source-reconciliation closure produces the stable unweighted index10/12, approximately83% (medium-low confidence), not83% of product implementation or tests. Next: review final E3/E5 reconciliations. Implementation remains **root Astra → Sol leads → approved workers** after full audit acceptance.
