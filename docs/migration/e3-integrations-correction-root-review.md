# Integrations correction — root review

Root accepts the finite **95-method owning-body correction**, with the qualifications below. This supersedes the earlier rejection of those95 evidence rows, not complete integrations/E3/E5 acceptance. The frozen candidate stays unchanged; [machine evidence](e3-integrations-correction-root-review.json) records verification, exact samples and lifecycle.

The combined check verified264 original method identities,1253 original ranges and434 overlay ranges against787 distinct pinned source files. All four frozen reports, original assertion associations and9037-file/46-package allocation remain unchanged. The overlay has356 new/reread ranges and78 borrowed ranges (76 explicit aggregate references plus two borrowed subranges). Of the95 old primary anchors,66 covered a whole body,19 were partial and10 pointed to the wrong symbol. Neither counts nor valid hashes alone prove semantics.

Root read the complete95-row correction narrative and independently inspected Jira connection/status/storage/mutations, GitLab update and quota bodies, GitHub auto-merge/quota/discovery and actual RPC/runtime bindings. No claim of personally rereading all787 files is made.

## Qualifications governing implementation

- **Exact null location:** both replacement hashes are valid, but the overlay's `previousLedger` paths point at aggregate records that already had hashes. The actual old nulls are `contracts.json#/methods/126/leafSourceEvidence/1` and `#/methods/256/leafSourceEvidence/2`. The verifier now checks those records explicitly.
- **Discovery limits:** organization queries request20 projects; the local response loop allows40 valid entries. A requested server page size is not local response validation. Viewer40 and organization20 have local counters; there is no per-organization continuation.
- **Partial effects:** Jira may store a token and mutate cache before site-file failure. Its plaintext fallback and requested0600 mode are not approved confidentiality guarantees. GitLab's empty-update early return still follows project resolution. Parse failure after a provider mutation is not rollback.
- **Auto-merge:** the merge-queue CLI path can merge immediately. Only the separate GraphQL path requests auto-merge. Preserve this distinction in confirmation and integration tests.
- **Linear:** the earlier root correction remains authoritative: acquire is awaited; returning the fetch promise without await releases early and bypasses the local asynchronous catch. Prior10 original and4 supplemental tests were not rerun or counted again.

## Remaining work

Account launch/residual and provider-runtime joins are already accepted by their separate root reviews; old active/pending wording is historical. Root still must reconcile the complete integrations result set and E3/E5 boundaries, including E5's explicit rights/notices/recordings/service decisions. Preserve all original baselines, faithful ports, behavioral RED–GREEN and real native/provider/SSH/folder/skew obligations.

The correction Task delivered and was officially released before acknowledgment. Release retained an external terminal without process action; no child was created. No product code, provider operation, test, installation or publication occurred in this review. Audit remains **10/12=83.3%, delta0**, medium-low confidence, high flexible24-hour risk and no defensible ETA. Actual Sol leads/workers remain behind complete root audit acceptance.
