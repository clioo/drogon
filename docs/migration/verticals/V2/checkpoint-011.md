# V2 checkpoint-011: layout matrix + gate wiring

Vertical / scope / base SHA / head SHA / branch / worktree:
V2 desktop/settings; responsive shell matrix (leaf) + immediate
withhold render guard + fail-closed bridge wiring (leader). Base
d8a7107; head this commit; branch
codex/vertical-02-desktop-settings; worktree
/Users/carlos/orca/workspaces/Drogon-rewrite/codex-vertical-02-desktop-settings.

Capabilities and original contract/test IDs:
ROOT CSS-layout breadth direction; ROOT availability review
(explicit-withhold fence). Handoff delivery 3 (layout matrices).
STYLEGUIDE tokens preserved.

Changed paths; shared-file patch requested:
main.css (M, +7): .workspace-heading strong ellipsis rule
mirroring sibling .path; zero token values changed.
probes/keyboard-focus-cdp.mjs (M): layout-matrix section
(1440/760 x light/dark overflow, 760 action hit-testing, form
cycle, inspector <=1100px behavior).
App.tsx (M): explicitWithhold render guard (present status
without files.v1 unmounts render immediately, even mid-busy;
status null is the only transient); createGatedFileBridge wired
into registration with effect-updated ref (never during render)
+ documented one-commit bound; keep-alive comment corrected to
match implementation (no terminal-routing unmount claim).
No shared-file edits; no V3 edits; no patch requested.

Baseline evidence; behavioral RED; GREEN commands + exit codes/counts:
Layout RED honest twice: transient disabled-pointer artifact
(probe fixed to wait settled shell) then genuine heading spill;
GREEN after fix. Combined (apps/desktop, Node24): vitest 31
files/338 passed, exit 0; tsc clean. CDP probe: 34 checks PASS
incl. hidden-mount, theme matrix, exact cleanup, shutdown fence.

Integrated/rendered/platform evidence; exact package identity if applicable:
Real drogond + dev Electron CDP, darwin; shots .preflight/
(gitignored). files.v1 still unadvertised (gate starts closed).

Unverified, missing, source defects, deliberate approved deltas:
V3 draft-state hoisting still their item (accepted residual on
true capability loss). No deliberate deltas.

Dependencies requested; next bounded checkpoint:
Bots plan sent, assembly grants pending (bab4cdf + 28c1c10
cherry-picks, 053c89f bots tree). CSS lane continues or Bots
on grant.

Owned processes: session/incarnation/host, settlement/release receipts:
Layout GLM ctx_f48f016243a6 accepted/inspected/re-verified,
release retained (leader terminal). Probe processes clean, no
strays. No active leaves; GLM terminal retained live.

Rollback; data/credentials/privacy check:
git revert of this checkpoint commit. Fixture /tmp only. No
credentials, no real user data.

Ready for independent review: yes (not self-accepted).
