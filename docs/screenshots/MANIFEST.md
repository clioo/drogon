# Drogon documentation screenshots

Generated by `node scripts/doc-screenshots.mjs` against the packaged `origin/main` build.
All content comes from the disposable Atlas Demo fixture; the Electron window is background-only and usage values come from the offline fixture.

| File | Surface and visible state | Seeding step |
| --- | --- | --- |
| `workspace-light.png` | Workspace backbone at 1440px with Atlas Demo/docs-preview, a live terminal tab, Explorer and fixture usage status bar (light). | createFixtureWorld() → `project add --name Atlas Demo` → `worktree create --name docs-preview --base main` → UI New terminal → terminal fixture command. |
| `workspace-narrow-light.png` | Workspace backbone at 760px light layout with the fixture terminal and responsive shell. | createFixtureWorld() → seedProject() → `worktree create --name docs-preview` → UI New terminal → terminal fixture command → Playwright setViewportSize(760, 1000). |
| `orchestrator-off-light.png` | Work Graph Orchestrator with Main agent, Ready to run, three approved fixture runtimes, fallback and optional modes off (Direct mode in the current build). | seedGraph() → CLI RPC `graph.write_policy` with `adversarial.enabled=false`, three approved fixture runtimes and an OpenCode fallback → UI opens Work Graph. |
| `bots-expanded-light.png` | Bots page with Lumen expanded, Nova and Sable cards, Release watch automation and a fixture-org/atlas-demo monitor. | seedBots() → UI creates Lumen, Nova and Sable → UI adds dormant Release watch responsibility → CLI `bot watch-pr --repo fixture-org/atlas-demo --manual` creates `github_pr.v1` monitor. |
| `meetings-filtered-light.png` | Meetings transcript list with accessibility search and Last 30 days filter applied. | createFixtureWorld() writes two Markdown transcripts → UI Meetings search `accessibility` → UI Meeting date `Last 30 days`. |
| `meeting-actions-light.png` | Atlas planning sync open with verified decision and extracted action item. | createFixtureWorld() writes Atlas planning sync → UI opens transcript → UI invokes `meeting.analyze` via Suggest actions → light capture explicitly clicks Add to my actions (dark reopens the verified analysis). |
| `automations-editor-light.png` | Automation editor showing Release readiness digest, UTC timezone, dormant Feb-29 cron schedule and enabled state. | seedAutomations() → CLI RPC `automation.create` for two enabled automations with dormant `0 4 29 2 *` cron → UI edits Release readiness digest. |
| `automations-runs-light.png` | Automation Runs dashboard with two recorded fixture runs. | seedAutomations() → CLI RPC `automation.run_now` for both automations → poll `automation.history` until both runs are `completed` → UI opens Runs. |
| `tasks-prs-light.png` | Tasks GitHub PR mode with review, checks and merge cells populated from fixture-org/atlas-demo. | createFixtureWorld() adds origin `fixture-org/atlas-demo` → fake `gh` serves PR #42/#37 JSON → UI Tasks selects PRs mode. |
| `settings-appearance-light.png` | Settings Appearance pane showing the active theme control. | createFixtureWorld() installs local fake `pi`, `claude`, `codex` and `opencode` binaries → UI selects Settings → Appearance/Agents; theme is selected explicitly for the filename. |
| `settings-agents-light.png` | Settings Agents pane showing detected fixture harnesses and run-locally controls. | createFixtureWorld() installs local fake `pi`, `claude`, `codex` and `opencode` binaries → UI selects Settings → Appearance/Agents; theme is selected explicitly for the filename. |
| `workspace-dark.png` | Workspace backbone at 1440px with Atlas Demo/docs-preview, a live terminal tab, Explorer and fixture usage status bar (dark). | createFixtureWorld() → `project add --name Atlas Demo` → `worktree create --name docs-preview --base main` → UI New terminal → terminal fixture command. |
| `orchestrator-off-dark.png` | Dark Work Graph Orchestrator with Main agent, Ready to run, approved runtimes and fallback while adversarial testing is off (Direct mode in the current build). | seedGraph() → CLI RPC `graph.write_policy` with `adversarial.enabled=false`, three approved fixture runtimes and an OpenCode fallback → UI opens Work Graph. |
| `orchestrator-adversarial-light.png` | Light Work Graph with dashed Adversarial loop (Adversarial test to Code review), Repeat up to 2×, Maximum iterations stepper and Ready to merge. | seedGraph() → UI adversarial toggle → CLI RPC `graph.write_policy` with `adversarial.enabled=true` → `graph.orchestrator_start` → poll `graph.orchestrator_status` until `passed`. |
| `settings-appearance-dark.png` | Settings Appearance pane showing Dark theme selected. | createFixtureWorld() installs local fake `pi`, `claude`, `codex` and `opencode` binaries → UI selects Settings → Appearance/Agents; theme is selected explicitly for the filename. |
| `settings-agents-dark.png` | Dark Settings Agents pane showing detected fixture harnesses and run-locally controls. | createFixtureWorld() installs local fake `pi`, `claude`, `codex` and `opencode` binaries → UI selects Settings → Appearance/Agents; theme is selected explicitly for the filename. |
| `orchestrator-adversarial-dark.png` | Dark Work Graph with dashed Adversarial loop, Repeat up to 2×, Maximum iterations stepper and the passed Ready to merge terminal. | seedGraph() → UI adversarial toggle → CLI RPC `graph.write_policy` with `adversarial.enabled=true` → `graph.orchestrator_start` → poll `graph.orchestrator_status` until `passed`. |
| `bots-expanded-dark.png` | Dark Bots page with Lumen expanded, Nova and Sable cards, Release watch automation and fixture-org/atlas-demo monitor. | seedBots() → UI creates Lumen, Nova and Sable → UI adds dormant Release watch responsibility → CLI `bot watch-pr --repo fixture-org/atlas-demo --manual` creates `github_pr.v1` monitor. |
| `meetings-filtered-dark.png` | Meetings transcript list with accessibility search and Last 30 days filter applied. | createFixtureWorld() writes two Markdown transcripts → UI Meetings search `accessibility` → UI Meeting date `Last 30 days`. |
| `meeting-actions-dark.png` | Atlas planning sync open with verified decision and extracted action item. | createFixtureWorld() writes Atlas planning sync → UI opens transcript → UI invokes `meeting.analyze` via Suggest actions → light capture explicitly clicks Add to my actions (dark reopens the verified analysis). |
| `automations-editor-dark.png` | Automation editor showing Release readiness digest, UTC timezone, dormant Feb-29 cron schedule and enabled state. | seedAutomations() → CLI RPC `automation.create` for two enabled automations with dormant `0 4 29 2 *` cron → UI edits Release readiness digest. |
| `automations-runs-dark.png` | Automation Runs dashboard with two recorded fixture runs. | seedAutomations() → CLI RPC `automation.run_now` for both automations → poll `automation.history` until both runs are `completed` → UI opens Runs. |
| `tasks-prs-dark.png` | Tasks GitHub PR mode with review, checks and merge cells populated from fixture-org/atlas-demo. | createFixtureWorld() adds origin `fixture-org/atlas-demo` → fake `gh` serves PR #42/#37 JSON → UI Tasks selects PRs mode. |

## Not captured

The authoring-canvas surface is not present in the packaged build: `WorkGraphPane.tsx` mounts only the daemon-owned `OrchestratorCanvas`; the repository contains the pure designer model/probe but no renderer route or entry point for a node inspector. No placeholder image was fabricated.

## Reproduce

```sh
./scripts/build-main.sh && /Users/carlos/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/doc-screenshots.mjs
```
