# Five-vertical execution window

## Latest staffing correction — 08:49 UTC

Carlos changed all five leaders to **Pi / `muse-code` / `muse-spark-1.3`**.
His subsequent reasoning requirement is **max**, explicitly launched with
`pi --provider muse-code --model muse-spark-1.3 --thinking max`.
Installed `pi --help` confirms `max` is a supported CLI value; the actual
provider's effective level still requires verification during smoke/startup.
Do not silently clamp or downgrade it.
The repeated five-leader instruction governs the one later reference to three.
Coding leaves may use Sonnet 5, GLM-5.3-Flash and Muse Spark 1.3, with load
distributed by each leader. This supersedes all Sonnet-as-leader language below,
not the five verticals, closure-first order, two generations or clock anchor.
The installed Pi catalog lists the exact provider/model; actual tool use,
Orca child dispatch and completion must still pass a bounded smoke.
Use explicit per-invocation provider/model, never Pi's default Qwen or a silent
Contributor variant. Pi/Qwen and experimental inference remain suspended.

Carlos authorized this organization on 2026-09-07, after receiving
[the complete vertical handoff](five-vertical-handoff.md). This overrides its
proposal-only staffing and the earlier three-direct-worker limit for the new wave.
It does not waive product parity, E5 decisions or independent acceptance.

## Order and authority

1. Finish the pre-existing native coordination wave first: retain active workers,
   review their deliveries, close reproduced defects, test, open PRs, integrate
   accepted work and settle related sessions without discarding files.
2. Then create five independent Orca-managed worktrees/branches from one verified
   integrated seed. One actual Pi/Muse Spark 1.3 max leader owns each V1–V5 vertical.
3. Root is director: architecture, shared-file ownership, cross-vertical contracts,
   integration requests, PR/merge order, combined testing and installed acceptance.
4. Each Pi/Muse leader delegates implementation to Sonnet 5, GLM-5.3-Flash or Muse Spark 1.3,
   chooses disjoint bounded tasks, reviews/tests their work, and publishes small
   verified vertical checkpoints. Leaders do not hand off review responsibility.
5. Exactly two delegated generations: root → vertical leader → leaf. Leaves do
   not delegate. Verify the actual Orca depth setting with a bounded launch;
   never bypass a denial through another Run/identity or change global settings.
6. The latest explicit choice is five Pi/Muse leaders; an unavailable exact
   provider/model/reasoning level requires reporting before substituting a leader.
   Preserve its exact worktree, WIP, task map and children; perform a stable explicit
   ownership transfer, never leave two leaders writing the same vertical.

Use exact approved providers: `claude-sonnet-5`,
`zai-coding-plan/glm-5.3-flash`, `opencode-go/muse-spark-1.3-contributor`.
Verify current availability and per-invocation unattended permissions. No silent
provider fallback, subscription/global permission change, Pi/Qwen or extra depth.
An observed model limit is different from a normal long-running test or idle TUI.

## Eight-hour plan

Clock anchor at first coordinator verification: **2026-09-07 08:41:33 UTC**.
Target: **2026-09-07 16:41:33 UTC** (10:41:33 America/Ciudad_Juarez).
The window includes closing the old wave; do not silently restart it at launch.
This supersedes the previous 13:49:44 UTC target as the user's new planning window.

| Elapsed target | Deliverable / decision |
|---|---|
| 0–1h | Close old native wave, PR/tests, accepted seed and settled ownership |
| 1–2h | Five leaders active, child lifecycle proved, interfaces and first vertical checkpoints |
| 2–5h | Parallel feature/test deliveries, integrated every reviewable checkpoint; domain breadth tracked against all46 packages |
| 5–6h | All five vertical heads composed; no new speculative subsystem starts |
| 6–7h | Cross-vertical journeys, failures/recovery, platform evidence, source parity gap review |
| 7–8h | Fix release blockers, exact packaged/installed acceptance, rollback and honest final gap report |

These are control points, not a claim that the remaining full scope is known to
fit eight hours. If closing the old wave or required platform/rights work consumes
more time, report the impact; do not drop capabilities, weaken tests or relabel
fixtures as production evidence. Start integration early, not at hour six.

## Capacity and checkpoints

Each leader starts with up to two useful, disjoint leaves, then increases only
when there is another bounded independent task and actual host/provider capacity.
Five leaders may therefore begin with up to ten leaves, not an unbounded swarm.
Report active descendants to root; extra leaves must not collide on module wiring,
lockfiles, build output, the same test fixture/service or another vertical's files.
Reuse settled terminals when safe. Prefer a few substantive leaf tasks over many
tiny workers spending most time rereading context.

Request a reviewable checkpoint roughly every30–60 minutes when meaningful work
is ready. Each has source/head SHA, file scope, baseline/RED/GREEN, consumer tests,
unverified gates and rollback. Root serializes integration and verifies the combined
head. A worker report is never independent acceptance or process-exit evidence.

Keep all46 package obligations and the source audit11/12 (91.7%, medium confidence)
visible, separately from product completion. E5 publication/recording/notices/service
choices remain open until explicitly resolved. Source-audit progress currently has
delta0; native candidate tests do not close E5. No defensible full-product percentage
or guaranteed eight-hour ETA is established by staffing alone.

## Git and release

Leaders own commits/pushes and small PRs for their vertical's assigned paths;
leaves do not change Git state unless a specific task grants a scoped action.
Root owns main, shared contracts, merge queue and final publication/installation.
No force-push or destructive worktree cleanup. Maintain one current integrated
candidate and preserve the last verified preview and all user sessions/data.
No cron/autonomous merge is enabled by this instruction: root supervises the loop.
