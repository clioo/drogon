# P-ACCOUNT-STATE: final raw-provider input handoff

This is a source-only recommendation for the **nine assigned provider entry files**, subject to root review. Their raw auth selection, parsing, request/fallback, partial-success and error contracts are recorded in [provider-inputs-final.json](provider-inputs-final.json). The accepted **7e7544a local cycle/cache/publication contract remains unchanged**. This does not accept E3, tests, provider behavior or the rewrite.

Audit status remains **10/12 = 83.3%, medium-low confidence, delta 0**. Next milestone is root review and integration of this handoff with separately assigned account launch materialization; the flexible 24-hour target remains high risk, without a defensible ETA. No Git, tests, builds, installs, native probes, credential reads, provider requests, services, source edits or child delegation occurred.

The frozen platform assignment remains 21 groups /126 method identities. No census was repeated. All original allocation references and 47 inherited BM/R/F/D gates are carried unchanged in JSON. Whole and inclusive-range SHA256 values identify current bytes, not behavior or supplied-pin verification. Convention: raw `read_bytes()`, ranges concatenate `splitlines(keepends=True)[first-1:last]`, retaining original terminators and any absent final newline. Root owns verification against supplied pin `c97906287bb7a390b25e2025b600d9fb3c25d9c3`.

## Reviewed results and ownership

| Contract | Concrete result boundary |
|---|---|
| PI-CLAUDE-AUTH / WIRE | Active scoped/legacy/file selection and delegated CLI repair differ from inactive app-owned refresh; successful HTTP can produce all-null usage. |
| PI-PTY | Claude /usage and Codex /status are bounded-tail text parsers with partial results, different timeout settlement and possible CLI-side auth/trust effects. |
| PI-CODEX-AUTH / WIRE | Presence is not token validity; WSL backend-first, RPC and PTY have different schemas, durations, errors and supplements. |
| PI-CODEX-RECOVERY | Pending index admission schedules a separate local supervisor before returning usage error. |
| PI-GEMINI | Explicit opt-in; first auth source controls fallback, per-call OAuth rotation and permissive quota deduplication. |
| PI-KIMI | Read-only local token source, delegated refresh, permissive numeric normalization and nearest-duration selection. |
| PI-GROK | Preferred issuer suppression, delegated refresh, weekly confirmation and conditional monthly fallback. |
| PI-MINIMAX / STORE | Cookie normalization, shared browser/manual transport, fixed-duration snapshots, encrypted/plaintext credential envelope and partial persistence effects. |
| PI-OPENCODE | Auth-cookie allowlist, sequential setup, workspace discovery and limited Flight-like text parser. |
| PI-TRANSPORT | Central versus provider-custom proxy behavior, limited error redaction and actual host/native boundaries. |

The JSON contains the full finite branch statements and exact source/test associations for each row; the following are the main review-sensitive distinctions.

**Claude.** [src/main/rate-limits/claude-active-usage-fetch.ts L1–248](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/claude-active-usage-fetch.ts:1) and [src/main/rate-limits/claude-oauth-credentials.ts L1–160](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/claude-oauth-credentials.ts:1) distinguish missing token, refresh-only, Keychain unavailable, unresolved WSL and live managed refresh ownership. Active usage accepts a nonempty untrimmed string token and ignores local expiry. A scoped host-system stale token may retry a different legacy token; managed/WSL sources cannot. CLI repair rereads credentials before retrying OAuth, but a failed repair may reach another CLI attempt.429 never becomes successful PTY fallback; Retry-After metadata is positive/capped24h and absent when invalid.

[src/main/rate-limits/claude-managed-account-usage.ts L1–104](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/claude-managed-account-usage.ts:1) and [src/main/claude-accounts/oauth-refresh.ts L1–170](/Users/carlos/Documents/Drogon-mentu-session/src/main/claude-accounts/oauth-refresh.ts:1) show the distinct inactive path: provider rotation can precede caller abort or failed write, and failed persistence is swallowed while the refreshed token is used. Managed ownership resolution can itself adopt a marker; it is not a pure read. Preview staging overwrites then deletes a scoped Keychain item without restoration, and panel trust compares shared percentages within one point, not account identity/reset time. Windows omits the preview panel. These are source behavior/risk findings, not executed account demonstrations.

[src/main/rate-limits/claude-oauth-usage-request.ts L1–103](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/claude-oauth-usage-request.ts:1) casts response JSON without a root schema. Its Fable search ignores is_active; malformed display names can throw. The previously accepted window mapper is reused, including its recorded limitations. All-null successful responses remain possible. Proxy setup precedes the10s HTTP timeout; errors/provider message strings are not uniformly scrubbed.

**CLI text and Codex.** [src/main/rate-limits/claude-pty.ts L1–354](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/claude-pty.ts:1) keeps100000 characters, automatically answers a recognized trust prompt, and can return partial success at exit/25s timeout. [src/main/rate-limits/codex-pty-rate-limit-probe.ts L1–236](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/codex-pty-rate-limit-probe.ts:1) uses a15s timer and500ms quota settle; timeout is error while exit can retain partial data. Percent/reset grammars have finite labels/lookahead, integer parsing, timezone and date-rollover quirks. Optional missing windows remain null. Disposer/write failures and requested child termination do not prove successful cleanup or exit.

[src/main/rate-limits/codex-rpc-rate-limit-probe.ts L1–301](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/codex-rpc-rate-limit-probe.ts:1) has unbounded JSON-line buffering, ignores malformed/mapping-exception lines, and can therefore time out on malformed matching data. Init-error replies advance; duplicate init replies may resend reads/reset deadlines. Missing result can map ok/null windows. Classification tolerance, canonical RPC versus actual backend window duration and full credit objects are described separately; the existing classifier test only checks percentages.

[src/main/rate-limits/codex-backend-auth.ts L1–98](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/codex-backend-auth.ts:1) does not validate truthy token/account-id types or refresh tokens. The backend uses native fetch, while other providers generally use Electron net/session fetch. Credit supplements reread auth and can preserve the primary result when failing. Reset consumption validates trimmed key presence but sends original text, has no local retry and can reject after a provider-side mutation; external idempotency is not proved.

[src/main/rate-limits/auth-filesystem-operation.ts L1–175](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/auth-filesystem-operation.ts:1) distinguishes a timed-out waiter from still-running UNC I/O. WSL operations share one slot per normalized distro and two global slots; abandoned queued work can be removed, but native work retains a slot until actual settlement. In-process home locks neither cancel their queue nor serialize real user terminals.

[src/main/codex/codex-state-db-backfill-recovery.ts L1–320](/Users/carlos/Documents/Drogon-mentu-session/src/main/codex/codex-state-db-backfill-recovery.ts:1) was read only to resolve the named side effect reachable from provider admission. The caller schedules a deduplicated supervisor, rather than waiting for index completion. It has five-spawn/one-hour loop budgets and retained gave-up suppression, with error-event versus actual-exit ambiguity. Its existing local lock/SQLite/CLI ownership is retained; no new recursive audit denominator or certification was inferred.

**Gemini, Kimi and Grok.** [src/main/rate-limits/gemini-usage-fetcher.ts L1–248](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/gemini-usage-fetcher.ts:1) selects auth.json before own OAuth credentials; malformed/access-denied first files stop fallback, and explicit opt-out performs zero auth/network reads. Own refreshed access credentials are saved on refresh paths but newly returned refresh tokens are discarded; auth.json rotation is not persisted. Project exceptions differ from successful empty project strings,401 is a single retry, and per-request10s signals are not a whole-flow deadline. Empty/all-invalid buckets can be ok with[] and null session. The local bundle extractor was read as code only: no installed bundle constants or real auth files were queried.

[src/main/rate-limits/kimi-fetcher.ts L1–355](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/kimi-fetcher.ts:1) leaves rotation to CLI, accepts Number coercion rather than integer-only fields, chooses nearest duration with stable first tie, and can classify malformed nested payload as network failure. Nonfinite expiry has asymmetric stale/fresh behavior. [src/main/rate-limits/grok-auth.ts L1–141](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/grok-auth.ts:1) sanitizes auth-read failures and suppresses alternate issuer resurrection when a preferred tokenless entry exists. Invalid expiry becomes unknown/fresh. Grok weekly zero is inferred only for undefined percent with confirmed period bounds; null is different. Monthly parseFloat accepts numeric prefixes, monthly fallback requires initial config, and successful weekly output omits monthly. Other transport error messages remain raw.

**Cookie providers.** [src/main/rate-limits/minimax-request-context.ts L1–203](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/minimax-request-context.ts:1) performs parallel cookie writes on a fixed shared partition. One rejected write can trigger cleanup before other writes finish; concurrent calls can race as well. Manual fallback shares the15s signal but is not used merely for HTTP non-ok, and cookie setup/cleanup is not bounded by it. An optional unvalidated endpoint can receive the manually forwarded cookie; external reachability of that option is not claimed here. MiniMax snapshots validate start time without using it, force300-minute duration, accept numeric strings and distinguish JSON parsing failures from later malformed shapes that become network errors.

[src/main/minimax/minimax-cookie-store.ts L1–165](/Users/carlos/Documents/Drogon-mentu-session/src/main/minimax/minimax-cookie-store.ts:1) explicitly supports plaintext fallback, stale process cache and hasCookie existence without validity. Encryption/ACL mock assertions do not prove effective OS confidentiality. Reused E4-F29 preserves Windows ACL fail-open/cache limitations. Save can fail before cache publication; clear empties cache before a failing file removal.

[src/main/rate-limits/opencode-go-request-session.ts L1–107](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/opencode-go-request-session.ts:1) serializes cookie writes within a call, fixing the corresponding intra-call race, but does not isolate simultaneous calls to the same partition. [src/main/rate-limits/opencode-go-page-scraper.ts L1–172](/Users/carlos/Documents/Drogon-mentu-session/src/main/rate-limits/opencode-go-page-scraper.ts:1) balances braces without full string grammar and reads numeric prefixes; the10M-character guard occurs after response buffering. Workspace attempts each get15s with no total attempt/deadline cap. Invalid override validation follows cookie/proxy setup, so it does not imply zero earlier effects.

**Redaction and proxy.** [src/main/network/proxy-settings.ts L1–325](/Users/carlos/Documents/Drogon-mentu-session/src/main/network/proxy-settings.ts:1) applies a queued, session-memoized policy with credential separation; the bounded helper means three attempts, not a time bound. Provider custom session logic differs. Named proxy login matching and readiness are characterized, but application request-guard/login registration remains another owner’s local boundary. [src/shared/codex-auth-errors.ts L1–69](/Users/carlos/Documents/Drogon-mentu-session/src/shared/codex-auth-errors.ts:1) excerpts recognized auth text without token redaction. Generic Codex close output uses a separate excerpt/redact/sanitize chain, while multiple normal/provider/cleanup errors bypass it. The existing OpenCode “never logs cookie” case uses a harmless error and only checks returned text; it cannot establish arbitrary error scrubbing.

## Source residuals, joins and future acceptance

There is no remaining unread body in the **assigned provider-local parser/auth input set**. This statement is bounded: it neither declares every import reviewed nor turns these remaining local ownership joins into external internals.

- **PI-JOIN-CYCLE:** reuse accepted7e7544a and frozen [rate-limit-source-review.json](../../../rate-limit-source-review.json); root’s17 original cases and separate RATE-QUEUE-001 remain its execution evidence, not ours.
- **PI-JOIN-LAUNCH:** `prepareCodexRuntimeHomeForLaunch` and `ClaudeRuntimeAuthService.prepareForClaudeLaunch` are separately assigned and still pending delivery/review in this Dispatch. Received auth preparation is not materialization proof.
- **PI-JOIN-KEYCHAIN / ACL:** explicit S4-keychain and E4-F29 bodies retained with exact JSON pointers and fingerprints.
- **PI-JOIN-REDACTOR:** OBS-COL-06 / OBS-COL-INTEGRATION source characterization reused; no new acceptance claim for E5.
- **PI-JOIN-RUNTIME / REQUEST-GUARD:** command resolution/PATH, home lock primitives, SQLite wrapper, native termination and application guard registration stay named local owner boundaries, with existing allocations/residuals preserved. The provider request/result bodies do not certify those implementations.

Fifteen original test files’ **assertion bodies were read, none run**; JSON lists full-file source fingerprints, precise allocation links and what each actually asserts. Additional originals are explicitly pointer-only in this task. The original complete allocation, 47 inherited gates and all platform method associations remain frozen; fifteen is a review-evidence count, not a reduced test denominator.

Later acceptance must preserve the baseline, port and behavioral RED/GREEN distinction:

1. Run complete original allocations unchanged in disposable pinned fixtures; retain original assertions and record setup/compile failures separately. Read pointer-only bodies before porting.
2. Exercise each contract’s explicit malformed/null/omitted/partial/coercion and event-order cases in JSON. Require full output objects and property absence, not schema/type annotations or only admitted requests.
3. Keep source-characterization fixtures separate from intended corrections for PI-DEF-ROTATION, PREVIEW, COOKIE-RACE, PARSE, REDACTION and inherited ACL behavior. Root must approve intended changes before actual Sol implementation.
4. Verify timeout budgets against deferred setup/native work, and credential publication after remote success, using synthetic tokens/provider protocols only. No test here established native cancellation, provider authorization, server version compatibility, encryption, effective ACLs or cookie isolation.
5. Preserve mixed-version, folder, SSH/WSL host ownership and live/unverifiable/exited gates. Later disposable PTY, process-tree, Electron proxy/session, Keychain/safeStorage and filesystem tests must observe real outcomes; no local fallback or exit inference on loss of contact.

All frozen input fingerprints are retained in JSON. Upstream MIT/copyright provenance is preserved; no source, credential, destination configuration or private account content was copied. **Recommendation: root review this finite raw-provider handoff; retain named joins and all execution/correction obligations.**

