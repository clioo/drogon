# Active rewrite goal

Activated in Codex on 2026-09-05. This replaces the blocked pre-migration Mentu integration goal; it does not mark that work complete.

Build an independently usable open-source Drogon in this repository: Rust core, execution service and `drogon-cli`; Electron/React desktop preserving Orca's useful UX and tested contracts. Reuse responsibly with provenance and licenses. Preserve the previous implementation/history in `clioo/drogon-orca`, whose privacy remains pending supported fork detachment.

The coordinator owns architecture, contracts, integration and independent acceptance. Three leaf lanes use Orca orchestration: AGY `gemini-3.8-flash-medium`, Claude Code `claude-sonnet-5` medium and OpenCode `zai-coding-plan/glm-5.3-flash`. Use per-launch permission options recorded in the preflight report. No nested swarms or competing writers on shared files.

Complete lifecycle preflight, inventory keep/reuse/rewrite/defer decisions, establish protocol and CI, then prove one real Electron/CLI/service/harness slice before expanding parallel waves. Preserve persistence, reconnection, exact cancellation, folder workspaces, SSH host authority and mixed-version safety. Validate rendered UI with the Electron skill and Playwright CDP, then dogfood Drogon to orchestrate a real task without runtime delegation to Orca.

Aim for a functional rewrite in approximately 24 hours from implementation start, for Dev-Day on September 14, 2026. This is a target, not permission to omit tests or claim unverified parity. Keep platform gaps and feature gaps explicit. Preserve user changes and secrets; maintain reviewed commits, evidence, recipe status and handoff.

After migration acceptance, resume the Mentu/Bots/Meetings handoff and matched difficult with/without Mentu experiment using Pi + DGX Spark exclusively for experimental inference. Measure actual tokens; `inference_budget` enforcement is not a current requirement. Test pinned local Mentu PR builds without waiting for upstream approval. Necessary upstream PRs must be narrowly justified, tested and written in English. Existing task authority permits autonomous progress; materially new authority still requires discussion.

## Current execution

- Checkout: `/Users/carlos/Documents/Drogon-rewrite`.
- Orca Run: `run_d97e86c69c2e`.
- First wave: read-only source inventory and controlled lifecycle tests. Product implementation has not started at goal activation.
- Earlier completed smoke: see [preflight results](preflight-results.md).
