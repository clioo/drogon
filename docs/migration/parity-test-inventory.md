# Parity source-test inventory — tests-first migration baseline

Status: **inventory-only**. Nothing in this document or its JSON companion
means a test was migrated, executed, or passed. Generated by
`scripts/inventory-source-tests.mjs` (deterministic, read-only, `git
ls-files`-bounded to the current checkout; no all-ref scans, no config
evaluation, no test execution, no inference, no Git mutations).

- Source: `/Users/carlos/Documents/Drogon-mentu-session`
  (`--source`), branch `codex/mentu-session-approved`, HEAD
  `c97906287bb7a390b25e2025b600d9fb3c25d9c3`; tracked tree clean at
  generation time; 1 untracked file
  (`.mentu/plans/drogon-rewrite-preflight.md`, not a test asset).
- JSON companion: `docs/migration/parity-source-tests.json`
  (schema `drogon.parity-source-tests/2`; every entry carries POSIX path,
  byte count, raw-byte sha256, domain, runner, membership evidence with
  `verified:false`, and the static-marker note).
- Determinism: two consecutive runs produced byte-identical output (`cmp`
  clean; rerun artifact kept at `.preflight/parity-baseline/rerun-check.json`
  — retained, never deleted — for the coordinator to re-verify). A future diff
  requires reconciling the source checkout, generator revision and environment
  before porting; it is not by itself proof that source files changed.

Coordinator verification: see `tests-first-checkpoint.md` (22:40 UTC).
The current generator has **28 independently passing tests** after additional
pre-mkdir source-boundary and temp-suffix checks; all 9,038 recorded asset hashes
match source and a fresh rerun is byte-identical. These observations accept the
filename census, not runtime discovery, fixture closure or feature coverage.

## 1. Frozen denominator (complete, reconciled)

Independent filename census (`git ls-files`, patterns `\.test\.[^.]+$`,
`\.spec\.[^.]+$`, `\.e2e\.[^.]+$`) agrees exactly with the manifest:

| Candidate class | Independent count | Recorded | Unexplained missing |
| --- | ---: | ---: | ---: |
| `*.test.*` | 8 507 | 8 507 | 0 |
| `*.spec.*` | 465 | 465 | 0 |
| `*.e2e.*` | 6 | 6 | 0 |
| **filename candidates** | **8 978** | **8 978** | **0** |
| Swift-convention assets (extra) | 30 | 30 | 0 |
| Infra assets (configs/workflows/actions) | 30 | 30 | 0 |

Filter note (not a discrepancy): the dispatch's 8506/465/5 counts the
**JS/TS family only** (8 976). The two remaining candidates are non-JS:
`config/tsconfig.e2e.json` (recorded `config-adjacent` — a tsconfig
colliding with the `.e2e.` pattern, not a test) and
`native/computer-use-windows/runtime-render.test.ps1` (recorded
`unclassified` — a PowerShell test script with no discoverable runner;
no workflow reference found). Same files, differing filters — the earlier
count was not wrong, it was JS/TS-scoped.

The 112 previously-missing test-named files — incl.
`.github/actions/cloud-sql-rollout-lease/*.test.mjs`,
`docs/site/tests/*.test.mjs`,
`src/main/runtime/orca-runtime-tests/*.spec.ts` — are all present now with
explicit domains; zero unexplained missing remains.

## 2. Suite discovery map (from actual config bodies, not filenames)

Includes resolve against the config's **package root** (nearest ancestor dir
with a tracked `package.json`, else repo root) — decided per rule globally,
not per file. Every membership is `verified:false` by construction. Rank:
explicit selection first, then narrowest scope, then rule order; losers stay
visible as `alsoMatched` (566 entries, e.g. mobile/relay dual invocation).

| Runner | Config body (read) | Discovery facts extracted |
| --- | --- | --- |
| Vitest (root unit) | `config/vitest.config.ts` | 6 globs (`src/**`, `config/scripts/**`, `tests/tools/**`, `tests/e2e/**/*.unit.test.ts`); package root = repo root; `process.platform` branch unresolved. The root `*.unit.test.ts` include explicitly outranks the Playwright default for those 39 files (alsoMatched, not lost). |
| Vitest (computer e2e) | `tests/e2e/vitest.config.ts` | `tests/e2e/computer-*.e2e.ts`, package root = repo root; 5 files; `fileParallelism: false`. |
| Playwright (Electron e2e) | `tests/playwright.config.ts` (`testDir: ./e2e`, no `testMatch`) | testDir resolved config-dir-relative → `tests/e2e`. `testMatch` default taken from the **installed stock implementation** (`node_modules/.pnpm/playwright@1.59.1/.../lib/common/config.js`: `"**/*.@(spec|test).?(c|m)[jt]s?(x)"`), expanded explicitly — NOT spec-only. Limit recorded: the repo config uses the `@stablyai/playwright-test` fork, whose dist bundle was inspected without confirming or denying a delta to that default. 357 `.spec.ts` files selected; overlapping `.unit.test.ts` files stay Vitest-owned (explicit beats default). |
| Vitest (mobile) | `mobile/vitest.config.ts` | `src/**/*.test.ts(x)` expanded textually to `.ts`+`.tsx`; package root `mobile/`; 500 files. |
| Vitest (relay) | `cloud/apps/relay/vitest.config.ts` | `src/**` scoped to the relay package (66 files); **computed** Postgres project (`readdirSync` + body scan for `ORCA_RELAY_TEST_POSTGRES_URL`) not statically resolvable (recorded gap). |
| Bare `vitest run` packages | `relay-fence-broker`, `relay-ops`, `relay-contract` `package.json#test` (+ mobile/relay informational) | no `--config`, no operands → documented default include scoped to package cwd, flagged `defaulted`; 3+12+2 files. `pnpm -r test` expansion itself stays runtime work (recorded gap). |
| node:test (cloud/actions/docs) | `cloud/package.json#pretest/#test` (explicit `.mjs` lists incl. `cloud/../.github/actions/…` dot-dot paths, collapsed textually), `docs/site/package.json#test` | 63 cloud-node (incl. both action tests), 2 docs-node. |
| Swift | `native/computer-use-macos/Package.swift` (`testTarget` mentions counted) | 30 convention-inferred assets (`Tests/` dirs, `*Tests.swift`, `native/` prefix); no package graph resolved (recorded limit). |
| CI | 12 workflows invoking runners + 3 action definitions | 17 `--exclude` tokens in `unit-tests.yml`; dynamic matrices unresolvable (recorded gap). |

## 3. Domain breakdown (files, not runtime cases)

| Domain | Files | Runner (inferred, unverified) |
| --- | ---: | --- |
| unit-vitest | 7 857 | vitest (root config) |
| mobile-vitest | 500 | vitest (mobile config) |
| e2e-playwright | 357 | playwright (tests/playwright.config.ts) |
| imported-spec | 108 | via static import from a runner-reached file (helpers, not suites) |
| cloud-vitest-relay | 66 | vitest (relay config) |
| cloud-node | 63 | node:test / `pnpm -r test` |
| native-swift (+config) | 30 + 1 | swift test (convention-inferred) |
| vitest-default (3 cloud workspaces) | 17 | bare `vitest run` default include |
| e2e-computer-vitest | 5 | vitest (tests/e2e config) |
| docs-node | 2 | node --test (docs/site) |
| imported-spec-unproven-roots | 0 today | cycles/dead ends with no runner-reached root (mechanism live, none in tree) |
| fixture / config-adjacent | 1 / 1 | assets, not cases |
| unclassified | 1 | the `.ps1` above — kept, unresolved |
| runner-config / ci-workflow / ci-action | 14 / 12 / 3 | inventoried bodies, not cases |
| imported-spec-unresolved | 0 | — |
| **total** | **8 978 + 30 + 30** | of 23 989 tracked files |

Helper discipline: `.spec.*` files are classified by selection first —
explicit config beats bare-default beats import reachability; a default-only
standalone spec keeps its inferred membership (never silently unresolved);
importer chains must bottom out in a runner-selected file or the spec is
demoted to `imported-spec-unproven-roots` with `unprovenRoots` listed
(`src/main/runtime/orca-runtime-tests/*.spec.ts` stay `imported-spec` via
`orca-runtime.test.ts`, which the reliability gates invoke directly).

Marker correction: `caseMarkers` are static text occurrences that imply
nothing about runtime counts in either direction (empty `.each` tables,
comments, string literals, conditional registration). The prior report's
"strictly larger" claim is withdrawn. Hashing is over raw Buffer bytes
(`src/relay/dispatcher.test.ts`, NUL-bearing, verified byte-exact against
an independent sha256); text decoding is separate, binary files get
`caseMarkers: null` and are skipped as importers (recorded gap).

## 4. What the hardening pass fixed (review findings closed)

- Output safety is no longer leaf/lexical-only: the resolved parent is
  re-checked after creation, so symlinked ancestors and aliases into the
  source tree fail closed (regression-tested).
- One central `safeRead` covers config bodies, the unit workflow, candidate
  texts, the importer scan, infra reads, and config rereads — no path
  follows symlinks or swallows read errors silently; refused/unreadable
  infra becomes gap stubs (`infra:*`), never evidence.
- Staging is exclusive and unpredictable (`wx` + random suffix, 10
  attempts); a preexisting temp symlink fails closed (regression-tested via
  the `INVENTORY_STAGING_SUFFIX` test hook).
- Playwright default corrected from invented spec-only to the installed
  implementation's full pattern, with the fork-delta limit recorded.
- All fixture commits carry `Co-authored-by: Codex <noreply@openai.com>`
  (coordinator's prior commit line preserved; no repo commits made here).

## 5. Verification performed (exact results)

- `node --test scripts/inventory-source-tests.test.mjs` → **26 pass,
  0 fail** (CLI strictness ×10 incl. parent-into-source and temp-symlink
  refusal; happy path ×16 incl. denominator freeze, honest classification,
  verified:false flags, binary hash, POSIX paths, package scripts,
  playwright testDir, swift assets, dot-dot collapse, bare defaults,
  unmapped suites, explicit-beats-default, default-only specs, cycle roots,
  unreadable retention, byte-identical rerun). Fixture repos are tmpdir-only
  `git init/add/commit` with throwaway identity — never real-repo commits.
- Manifest regenerated twice from the legacy checkout → `cmp` clean
  (byte-identical; `.preflight/parity-baseline/rerun-check.json` retained).
- Independent census (`git ls-files` + grep per class) matches exactly:
  8507/465/6 with `unexplainedMissing: 0`; coordinator's independent
  8978-path/hash cross-check (zero mismatch) is consistent with this
  manifest's `recordedCandidates` and per-entry sha256.

## 6. Closure separation (what this does and does not prove)

- **Proven here:** candidate-file completeness — every test-named tracked
  file is recorded with a byte-exact hash; the denominator is frozen.
- **Not proven here:** runner selection closure (all membership is
  `verified:false` inference — only suite execution proves selection);
  CI-matrix closure (shards, containers, and `pnpm -r` expansion are
  runtime work); fixture/behavior closure (this inventory never runs a
  test). These need the tests-first execution waves, not more inventory.

## 7. Remaining unknowns (explicit gaps, not deferrals)

`candidates:unclassified-present` (1 ps1 — needs a Windows CI trace);
`cases:static-markers-only`; `cloud:pnpm-recursive`;
`imports:binary-unscanned` (1 file);
`playwright-discovery:tests/playwright.config.ts` (defaulted testMatch,
fork delta, env conditionals);
`reachability:inferred-not-verified` (global);
`vitest-include:computed:cloud/apps/relay/vitest.config.ts`;
`vitest-include:conditional:config/vitest.config.ts`;
`vitest-include:defaulted:*` (5 bare-run rules);
`workflows:dynamic-matrix`.

## 8. Regenerating

```
node scripts/inventory-source-tests.mjs \
  --source /Users/carlos/Documents/Drogon-mentu-session \
  --output docs/migration/parity-source-tests.json
node --test scripts/inventory-source-tests.test.mjs
```

Reruns must be byte-identical (`cmp` against a second output). Any diff
means the source checkout moved — the tests-first red flag before porting.
