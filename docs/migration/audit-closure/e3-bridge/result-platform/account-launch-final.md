# Account launch preparation: finite source join

The two assigned launch callbacks are characterized for independent root review: Codex home routing and Claude credential preparation now join their actual local materialization, restore, ownership and failure paths. This is a source handoff, not account/provider execution acceptance. **Audit remains 10/12 (83.3%), delta zero, medium-low confidence; 24-hour deadline risk remains high.** Sol implementation remains gated on the entire audit.

Source: `/Users/carlos/Documents/Drogon-mentu-session`, supplied frozen pin `c97906287bb7a390b25e2025b600d9fb3c25d9c3`. No Git, tests, builds, provider calls, runtime/account/config reads, process control or delegation occurred. Prior artifacts remain untouched. [Machine-readable contracts](account-launch-final.json) contain the exact two-entry reconciliation, ten contracts, thirteen assertion-body groups, all inherited allocation pointers, and 75 inclusive source ranges with whole/range SHA256.

| Entry | Actual returned result | Source anchor |
|---|---|---|
| `prepareCodexRuntimeHomeForLaunch` | Selected managed home string, or host real-home `null`; refusal rejects. Neither path nor null proves credentials/history/provider readiness. | `src/main/startup/codex-launch-preparation.ts:11–95` |
| `ClaudeRuntimeAuthService.prepareForClaudeLaunch` | Await serialized auth sync, then project current host/WSL config, env patch, strip flag and provenance; rejection may follow partial effects. | `src/main/claude-accounts/runtime-auth-service.ts:18–24,76–80` |

The entry whole-file hashes are respectively `dd10935c1064ca855fa5b622095da213cf6330952ea3cb3ed64ebcc3cbaa5ae7` and `ab8308d1b540f2a8e2667d63118e61aa9a17f36d65b6528773e925264afe1214`. Hashes use raw bytes; inclusive ranges retain original line terminators and final newline. The supplied commit pin was not independently authenticated with Git.

| Contract | Concrete result/state/failure disposition | Main source anchor |
|---|---|---|
| AL-C01 | Missing Codex service refuses. Workspace pre-trust failure warns. Real-home hook ensure can change routing to managed; it internally catches install/rollback failures. Managed hook error/rejection warns and still returns selected home. Hook fallback can follow partial writes. | `src/main/startup/codex-launch-preparation.ts:16–94`; `src/main/codex/codex-real-home-hook-install.ts:73–114,191–228` |
| AL-C02 | Host account ownership uses canonical root/account/home and exact regular marker. Indeterminate ownership refuses and retains selection; proven untrusted clears host legacy/map IDs. Matching unavailable-home auth check gives missing/unreadable/torn JSON 5s grace; settled incomplete/empty credentials can clear immediately. Unknown auth modes remain permissive. | `src/main/codex-accounts/runtime-home-service-managed-home.ts:79–121`; `host-codex-managed-home-ownership.ts:1–204`; `managed-codex-auth-readiness.ts:98–215` in the same directory |
| AL-C03 | Host default is real home only with POSIX shell-probe support, no custom override and usable hook gate; otherwise shared managed home. Deselection preserves external login/logout, fences managed shared bytes, and restores only according to actual provenance rules. Pending provenance precedes guarded auth write; failure may leave recovery state. | `src/main/codex-accounts/runtime-home-service-auth-sync.ts:1–292`; `runtime-home-service-auth-provenance.ts:1–224`; `src/main/codex/codex-real-home-path.ts:1–126` |
| AL-C04 | Eight resources use owned-link/copy markers and conservative indeterminate reads. Config promotion precedes mirror, handles baseline/conflicts and eight allowlisted settings, and stalls on unreadable state. Promotion, config mirror and baseline writes are not one transaction; WSL existing config is written in place. | `src/main/codex/config-settings-promotion.ts:208–358`; `config-settings-baseline.ts:37–120`; `codex-config-settings-upsert.ts:29–176` in the same directory |
| AL-C05 | WSL selects direct account/system home, captures it before awaiting drain, then requests background history work. Drain `void` means completed **or pending**; an apply failure can re-inspect into pending. Auth/MCP promotion can survive later failure. Hash/inode checks and recovery preserve late writers; no post-await account-generation check is present here. | `src/main/codex-accounts/runtime-home-service-launch.ts:1–154`; `legacy-wsl-runtime-auth-drain.ts:1–269`; `legacy-wsl-runtime-auth-drain-scripts.ts:1–300` in the same directory |
| AL-C06 | Claude untargeted account policy follows Windows account runtime; non-Windows stays host. WSL managed projection injects guest config path; system WSL uses empty env patch. Unparseable/missing guest home can produce host configDir with runtime WSL and null guest path. WSL projection omits host deferred-refresh flag. | `src/main/claude-accounts/runtime-auth/runtime-auth-preparation.ts:15–114`; `src/main/claude-accounts/runtime-paths.ts:1–34` |
| AL-C07 | Host/WSL ownership precedes credential reads. Verified refreshed identity persists to managed storage; ambiguous live bytes can be retained without overwrite. Live/seeded PTY state defers refresh. Successful provider rotation followed by persistence failure can fall back to old material. Selected WSL bytes are isolated, although outgoing host readback can occur before the WSL branch. | `src/main/claude-accounts/runtime-auth/runtime-auth-sync.ts:15–284`; `runtime-auth-managed-credentials.ts:24–144`; `runtime-auth-readback.ts:1–157` in the same directory |
| AL-C08 | Snapshot records independent file/scoped/legacy Keychain ownership. Restore orders OAuth metadata, credential file, then owned Keychains; external changes survive. Materialization orders credential file, scoped/legacy Keychain, then metadata. Rejection can follow partial effects; rollback itself can reject. Malformed config metadata write can return false without rejecting preparation. | `src/main/claude-accounts/runtime-auth/runtime-auth-snapshot-restore.ts:1–185`; `runtime-auth-snapshot-capture.ts:1–119`; `runtime-auth-runtime-state.ts:1–194` in the same directory |
| AL-C09 | Actual WSL wrapper reaches native spawn, returns code/null/stdout/stderr/timedOut, and drops outputTruncated. These account calls have no termination barrier: timeout can settle without proving the guest writer exited, so recovery must retain uncertainty. | `src/main/wsl/wsl-runner.ts:197–259`; `src/shared/child-process/run-process.ts:114–354` |
| AL-C10 | **Confirmed source join defect:** invalid/unowned existing Claude host account clears legacy ID only. Actual Store shallow merge preserves runtime-map host ID; selector prefers it and preparation can still report managed provenance. Absent-account and WSL cleanup use different paths clearing the map. | `src/main/claude-accounts/runtime-auth/runtime-auth-sync.ts:135–181`; `src/main/claude-accounts/runtime-selection.ts:29–64`; `src/main/persistence/applying-settings/settings-update.ts:236–266`; `src/main/persistence/loading-store/profile-preferences.ts:46–47,70–75` |

The account-map defect is source-inferred, not reproduced by running tests. Its precise fixture is an **existing** host account selected in both fields whose storage is unowned or credentials invalid/missing: expect legacy ID null, surviving map ID, and managed provenance/strip flag. A separate root-approved correction should clear both host fields while preserving WSL selection and ownership-aware restore. Likewise, separately characterize selection changes across awaits, provider rotation followed by failed persistence, and WSL timeout with a surviving writer. These are retained source behaviors/risks, not assumed fixes.

The direct startup helper had no matching original test reference in the scoped search. Existing body evidence remains useful but narrower:

| Original suite under `src/main/` | Inclusive body ranges read | What the bodies actually establish |
|---|---|---|
| `claude-accounts/runtime-auth-service-launch-refresh.test.ts` | 43–209 | System auth untouched; managed refresh persistence; live-PTY refresh deferral (that case calls shared rate-limit preparation). |
| `claude-accounts/runtime-auth-service-materialization.test.ts` | 56–214, 395–488 | Rematerialization, ownership rejection/legacy marker adoption, missing-snapshot cleanup, unchanged-check failure and mode600; permission cases return early on Windows. |
| `claude-accounts/runtime-auth-service-deselect-restore.test.ts` | 331–611 | External logout/unknown ownership preservation. **“Restores system snapshot” actually asserts stale bytes unchanged; “keeps missing-managed selection” asserts legacy selection null; “restores missing-managed oauth metadata” asserts unchanged metadata.** Preserve bodies, not stronger title interpretations. |
| `claude-accounts/runtime-auth-service-wsl-runtime.test.ts` | 42–275 | Account-policy/default-distro projection and missing-credential deselection. Mocked WSL homes do not prove actual UNC/bash ownership. |
| `codex-accounts/runtime-home-real-home-lane-routing.test.ts` | 57–224 | Real-home null versus managed path, hook gate/custom home and migration eligibility. |
| `codex-accounts/runtime-home-per-account-homes.test.ts` | 42–198 | Distinct account homes and actual resource/config materialization; its “concurrent” account test calls launches sequentially. |
| `codex-accounts/runtime-home-managed-auth-recovery.test.ts` | 39–214 | Missing/torn auth grace and healing; no resurrection from shared bytes; later host-only deselect. |
| `codex-accounts/runtime-home-resume-selection-gate.test.ts` | 135–176 | Locked marker refusal preserves ID; wrong marker clears both host fields. |
| `codex-accounts/runtime-home-wsl-managed-accounts.test.ts` | 317–434 | Waits mocked drain before bridge; unknown pane attribution is conservative. |
| `codex-accounts/legacy-wsl-runtime-auth-drain-apply-script.test.ts` | 65–146, 163–227 | Hash/inode rejection, interrupted partial promotion, source recovery and preservation of changed destination/recovery artifacts. |
| `codex/codex-real-home-hook-install.test.ts` | 281–340 | Exact-byte rollback, new-file removal, rollback failure as unavailable warning and cached unsupported retry. |

No assertion above was executed. Entire original suites and allocation membership remain mandatory, including account switching/readback/snapshot/Keychain, Codex default/provenance/resource/Windows/WSL/drain rollback, config promotion, and native WSL runner suites. The JSON preserves immutable ordered-array pointers and digests for WP-ENG-HARNESS, RUNTIME, SHELL, UI-SETTINGS, REMOTE and AGENTSVC, plus **all** inherited platform/runtime allocations and the original 33 test queues, 70 execution queues and 47 remaining acceptance rows. Body selections do not replace any denominator.

Reuse is exact: E4 `S4-keychain`, `E4-F13`, `C-CODEX`, and root-reviewed runtime `RRT-J01`; pointers and body hashes are in JSON. Keychain ownership is the process OS user. SSH remains an upstream owning-host route, not a new local account target; no cross-host credential injection is authorized by these results.

Residual scope is explicit: raw rate-limit provider result contracts remain with their separate lead; full TOML/path-parser semantics remain the existing C-CODEX local seam; broader unawaited account/history backfill and constructor migration/shell-parser contracts are not recertified here. Their exact local filenames and owners are recorded, not relabeled as external execution. Native Keychain, WSL guest filesystem/process, OAuth endpoint and SSH owning-host behavior require their isolated platform/provider tests.

Next obligations are whole unchanged source baselines in disposable capsules, exact assertion ports, candidate behavioral RED then GREEN, every listed error/partial-success/race fixture, and real packaged local/folder/Windows/WSL/SSH compatibility with `live/unverifiable/exited`. Setup failure is not behavioral RED, skip is not PASS, and provider mocks do not prove production outcomes. Integrations Dispatch `ctx_006b14bc1968` can consume AL-C01–10 now while retaining its own outer result/error contract; root alone accepts this finite join and the whole audit.

