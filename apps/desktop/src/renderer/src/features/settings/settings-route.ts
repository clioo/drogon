// Settings page route id. Settings is a local full-page route (no service
// capability): App mounts SettingsPage directly when this route is active,
// so unlike the capability-gated panels it needs no registry descriptor.
export const SETTINGS_ROUTE_ID = "settings";
