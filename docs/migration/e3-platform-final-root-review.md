# E3 platform: finite final-local source acceptance

2026-09-06. Root accepts the twelve `FL-*` contracts and eighteen AI Vault parser specializations in `audit-closure/e3-bridge/result-platform/final-local.md/json`, subject to the qualifications below. This is **not complete platform/E3 acceptance or executed product parity**. Source pin remains `c97906287bb7a390b25e2025b600d9fb3c25d9c3`.

## Verified scope

Root read the twelve result narratives, eighteen specializations, nine lead corrections, cross-owner dispositions and original assertion associations. Independent checks verified 114 tracked source files at the pin, 127 raw inclusive-line ranges, and 44 additional leaf ranges under their declared LF/no-final-newline convention. The report's “114 records” is not 114 ranges. All 126 original method pointers, 21 group identities, 186 final-contract joins, 16 original allocation records, 47 inherited gate records and reused Skills rules/assertion/defect/acceptance references reconcile. All 9,037 original file allocations across 46 packages remain required.

Ten of twelve input hashes match. The runtime input was an earlier snapshot and is superseded by accepted `7368bb7`; integrations is still active and its current bytes are not an accepted delivery. No historical snapshot was fabricated. Four final-local artifact hashes and exact root samples are recorded in the companion JSON. The old parent/leaf candidates are retained as provenance, not newly endorsed wholesale.

Preliminary verifier failures were schema assumptions: parent method identity is `group`, reused Skills use authority pointers, and `JOIN-*` references resolve through `crossOwnerJoins`. Corrected checks pass; these were not source defects.

## Independent source findings and qualifications

Root read seven full source/test files plus four explicit ranges of the native macOS implementation, eight distinct files total. These samples do not claim all native lines were independently reread.

- **Snapshot removal:** the identity guard belongs to FIFO/age pruning. TypeScript `forgetWindowTarget` directly removes computed aliases, so the report's general “stale alias removal checks identity” wording must not cover that path. TypeScript adds global canonical aliases even under explicit namespaces; Swift does not. Explicit lookups still avoid global fallback. Preserve stale-invalidation and namespace tests without claiming a reproduced exploit.
- **Parse-cache persistence:** schema admission is shallow and accepts array-valued sessions; appVersion need only be a string. Creation modes are not evidence of existing-directory hardening or Windows ACLs. Startup cleanup may remove another instance's pending temp save. Persistence and permissions remain execution obligations.
- **Gesture close race:** after the final point, close can clear either the next-send timer or the final grace timer without settling the Promise. The single original happy-path WebSocket assertion was read, not run; it does not cover either race. Sending frames does not prove a device acted on them.
- **Simulator boot:** polling can exhaust its deadline and resolve without observing `Booted`. Characterize this historical behavior separately from intended failure reporting; no real simulator was started.
- **Native text/actions:** successful AX replacement followed by failed readback can fall back to synthetic typing/paste after an effect. SetValue can genuinely report native readback verification; not all native actions are synthetic/unverified. AX page scrolling ignores individual false returns. Source review is not a live reproduction or permission to duplicate effects.

All original assertion allocations, twelve-contract discriminating tests, six platform execution obligations, prior 47 gates and source-defect/correction distinctions survive. No original/candidate tests, native commands, rendered checks, installs, provider calls or inference ran during this root review.

## Cross-owner resolution and next work

`JOIN-SPEECH` is accepted through `e5-final-root-review.md/json` at `2c682b0`; its exact input hashes match. The candidate's pending-root wording is stale, not a new speech source gap. `JOIN-AUTO` can reuse accepted runtime `7368bb7`, but its integrations/worktree outcome join remains open. `JOIN-ACCOUNT` reuses the accepted local rate-limit cycle at `7e7544a`; raw provider inputs and account launch preparation have fresh disjoint audit-only assignments.

Orca accepted provider task `task_ff8c47a44761` / `ctx_48840b69bf1e` and account-launch task `task_823552276b48` / `ctx_dee67d480e3d` in the released, actual Astra terminals. Each owns only two new report files, with no children, real credentials, provider requests or product changes. Integrations received exact coordination message `msg_114ceeab47f5`; no circular wait is required.

E5 generated/localization task `task_84e717dbf44a` delivered candidate `msg_54c340febd56` and was officially released with receipt `8861ca05-fa91-4186-ac32-10d745d01b51`: retained/external terminal, no process action. Its source-map/catalog claims await root review. Three audit tasks remain active at this checkpoint.

Audit stays **10/12 = 83.3%, delta 0, medium-low confidence**. E3/E5 stay open; no defensible ETA, high flexible 24-hour fidelity risk. Actual Sol implementation leads with approved workers begin only after complete source-audit acceptance.
